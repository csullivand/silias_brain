# 2026-08-11

## Silia — SL-1579 Manual escalation: depuración de la cadena de bugs

Sesión larga arreglando la escalación manual (SL-1579) al probarla en dev. Cinco bugs encadenados:
1. Guard `expectedStatus:'active'` → no bloqueaba conversaciones cerradas → bot seguía respondiendo. Fix: reactivar + saltar guard.
2. Adversarial BLOCK: reactivar sin limpiar `closedAt` → `memoryTtlCleanup` + TTL nativo BORRABA la conversación. Fix: reconciliar closedAt/expirationTime.
3. Duplicados: idempotencia por flag desincronizado → fix: idempotencia por la tabla de Escalaciones.
4. `CONVERSATION_NOT_FOUND`: el FE manda channelId, el backend buscaba por id → fix: `resolveChatConversation` (id o canal). + infra `CHAT_TABLE` env + IAM sobre `Chat`.
5. FE: ocultar botón si ya escalada.

**Bloqueo:** subir código manual no aplica env/IAM. Agregué CHAT_TABLE por CLI; el IAM sobre Chat no pude (sin `iam:PutRolePolicy`). Falta un `sam deploy` real que aplique todo.

Detalle: [[Claude Sessions/silia/SL-1579-manual-escalation-debug/2026-08-07]]