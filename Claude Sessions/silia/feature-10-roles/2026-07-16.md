# Sesión SL-1289 — Feature 10 + contadores + visibilidad + logging (2026-07-16)

Rama: **fix/SL-1289-default-roles-migration** (base: develop). 12 commits. PR: **NO abierto aún**.
Plan detallado: docs/feature-10-roles-plan.md. Review+adversarial: PASS ✅ / Approved with suggestions ⚠️.

## Commits (orden)
- 30afb6c96 feat(roles): custom-role data model + CRUD validations (alta)
- 8f4fa47b2 feat(roles): clone endpoint, catalog category, dedicated audit log (media)
- 6a6ae4818 feat(roles): account-aware resolution, effective-cache invalidation, >20 alert (baja)
- 9c07b854a chore(roles): structured logs (console) at key steps
- 4d6d00361 chore(roles): console logs instead of Pino (luego revertido)
- 8b6ef2a5b Merge develop
- 1322b894d fix(user): resolve "before initialization" (TDZ) en listOrgUsers
- daf7a36b6 feat(access,user): effective (inherited) counts en management + grant CRUD logs
- 2ec80b496 feat(teams): effective (inherited) counts en getTeamsByAccount
- 9c4ca2947 chore(access,roles): getLambdaLogger en vez de console en grant+role handlers
- ad23c679a fix(folders): visible_to=me fail-closed + diagnostics
- 21f7218cc refactor(teams): Folder/Chatbot via dynamic import memoizado (evita ciclo cold-start)

---

## DECISIONES (cerradas)

### Del PRD (resuelve el modelo de Feature 10)
- **Permisos atómicos (catálogo ~102) = GLOBAL** (definidos por Silia, enum en código + tabla resource). El cliente NO crea permisos, solo combina.
- **Roles = HÍBRIDO:** Super Admin/Implementador = sistema (cross-cuenta); Admin/Supervisor/Operador = **default globales** (no editables); **roles custom (Fase 2) = POR CUENTA** (name único por cuenta, límite 100/cuenta).
- Modelo elegido: **accountId nullable** — defaults con accountId=null + isDefault=true (globales); custom con accountId=<cuenta> + isDefault=false.
- **Aditivo, sin big-bang:** roles globales actuales quedan accountId=null y siguen resolviéndose por nombre. Cero ruptura en prod.

### Con el usuario
- Alcance del ticket = **solo backend** (modelo+seed+endpoints+validaciones+audit). Matriz-UI, /me/permissions (ya existe) y asignación (Feature 11) = aparte.
- **Feature flag de coexistencia código↔DB = N/A**: el provider (defineAbilitiesForRole) YA leía de DB; no había refactor pendiente.
- **Rutas: mantener las actuales** /role (singular) y /permissions/catalog (no romper FE). El ticket decía /roles y /permissions_catalog (nominal).
- **Contadores = EFECTIVOS** (con herencia): grant al folder padre cuenta descendientes.
- **Logger: usar getLambdaLogger(event)** (Pino SÍ funciona; confirmado con logs live de Teams y User).
- **Visibilidad: fail-closed** (no fail-open).

---

## IMPLEMENTACIÓN — Feature 10 (12 subtareas, todas hechas)

### Alta (commit 30afb6c96)
- **F10-S1** RoleModel (Roles/domain/models/Role.model.ts): +accountId nullable, isDefault, description, createdBy, deletedAt+ttl (soft delete 90d). save() OMITE accountId cuando es falsy (globales fuera del GSI, sin empty-string keys). Nuevos finders: findByAccountId, findByAccountAndName, countByAccountId. findById excluye soft-deleted. findAll/findByName filtran attribute_not_exists(deletedAt) y proyectan isDefault/accountId/description. Infra: GSI accountId-name-index + TTL en tabla role.
- **F10-S2** scripts/migrations/seed-default-roles.ts: UPSERT idempotente de DEFAULT_ROLES con isDefault=true, accountId=null. NO inventa el mapeo rol→permiso (matriz PRD pendiente).
- **F10-S3** role/create.ts: unicidad por cuenta→409 role_name_taken; límite 100→422 role_limit_reached (S8); permission_ids validados vs catálogo→422 invalid_permission_ids; crea filas permission (role_permissions); assertPermission(organization.roles.create).
- **F10-S4** role/delete.ts: immutable por isDefault→403 default_role_immutable; bloqueo si hay asignaciones→409 role_has_assignments {users,teams} (users por UserModel.countByRole por nombre + grants por AccessGrantModel.findByAccountId filtrando roleId, fail-open); si limpio→softDelete + limpia filas permission.
- **F10-S5** role/update.ts: immutable por flag; unicidad al renombrar; diff de permisos (add/remove) contra findAllByRoleId; audit before/after.

### Media (commit 8f4fa47b2)
- **F10-S6** role/clone.ts (NUEVO): "Copia de X" (+sufijo (2) si colisiona), copia filas permission, respeta límite. Nueva función CloneRoleFunction + ruta POST /role/{id}/clone en template + build script build:prod-Role-Clone (genera cloneMin.handler).
- **F10-S7** category en catálogo: ResourceModel +category; getCatalog devuelve {id,name,description,category}. Valor VACÍO hasta sembrar de matriz validada (no inventado).
- **F10-S9** audit dedicado: Roles/domain/models/RoleAuditLog.model.ts + Roles/application/services/RoleAuditLogService.ts (fail-open, espejo de BillingAuditLog). Tabla RoleAuditLog + GSI accountId-timestamp + env ROLE_AUDIT_LOG_TABLE. Wired en create/update/delete/clone.

### Baja (commit 6a6ae4818)
- **F10-S12** (DESBLOQUEA Feature 11) shared/config/permissions.ts + shared/middleware/requirePermission.ts: defineAbilitiesForRole/resolveRoleId ahora account-aware (custom por (accountId,name), fallback default global por nombre preferiendo isDefault). Cache key = (role, account) → evita envenenamiento cross-account. clearAbilityCache limpia todas las keys por prefijo de rol. assertPermission pasa callerAccountId. checkPermission/checkAllPermissions +accountId opcional.
- **F10-S10** invalidación efectivos: Access/domain/services/PermissionInvalidationService.onRolePermissionsChanged (sync ≤100, SQS >100). UserModel.listUserIdsByRole. update.ts la dispara cuando cambian permisos (fail-open). Envs REDIS_URL_SSM_PARAM + PERMISSION_INVALIDATION_QUEUE_URL (ImportValue) en template Roles.
- **F10-S11** alerta interna >20 roles custom: log estructurado ALERT en create/clone (sin webhook Slack inventado).

**Nota infra (cambio externo intencional):** el template de Roles ahora usa roles IAM DEDICADOS (RoleRolesCreate/Update/Get/Delete/Clone...) en vez del !Ref Role compartido — resuelve el punto de IAM.

Tests Feature 10: ~27 (roles CRUD + permissions.customRole).

---

## IMPLEMENTACIÓN — otros arreglos de la sesión

### Bug: contadores en /user/management siempre 0 + teams []
- **Root cause (log lo confirmó):** WARN listOrgUsers partial enrich failure, firstError="Cannot access uh before initialization" (51/55). = **TDZ/dependencia circular**: enrichUserWithTeamData hacía import() dinámico de modelos cross-módulo POR USUARIO dentro de Promise.all → ~55 imports concurrentes racearon la init circular.
- **Fix (1322b894d):** loadEnrichDeps() memoizado + secuencial (una sola carga ordenada) antes del Promise.all; pasar deps a los helpers. getTeamIdsForUser→import estático. console.error en catch para superficie el error real.
- **Lambda:** ${StackName}-user-list-org-users. Log group: /aws/lambda/dev-app-silia-com-user-list-org-users. Diagnóstico: INFO "listOrgUsers" (ok) vs WARN "listOrgUsers partial enrich failure" (firstError).

### Contadores EFECTIVOS con herencia (daf7a36b6 management, 2ec80b496 teams)
- Semántica: elevados→totales de cuenta; no-elevados→grants directos + descendientes de folders con grant + agentes dentro. Función pura expandEffectiveCounts (export en listOrgUsers) — expande por prefijo de path, robusta a path vacío. Tests: 5 casos incl. borde /f1 vs /f10.
- management: fetchAccountResources (folders+agents una vez/request) reusado para totales elevados y expansión no-elevados.
- teams: mismo patrón en getTeamsByAccount; nuevos envs FOLDERS_TABLE+CHATBOT_TABLE en template Teams. Folder/Chatbot cargados via dynamic import memoizado (loadResourceDeps, commit 21f7218cc) para evitar el mismo TDZ.

### Visibilidad fail-closed (ad23c679a)
- Bug: GET /folders?parentFolderId=ROOT&visible_to=me con operador traía TODO. Causa: el catch de getVisibleObjects era **fail-OPEN** (devolvía lista completa sin filtrar) → fuga de permisos.
- Fix: helper applyVisibleToMe en listFolders — centraliza los 2 call-sites; **fail-CLOSED** ([] en error); loguea skip reason (elevated_role/no_user_context), grantedObjects/grantedFolders/original/filtered counts, o el error real.
- FolderReadRole (ListFoldersFunction) SÍ tiene IAM+kms:Decrypt sobre AccessGrant → no era KMS.
- Diagnóstico post-deploy (log group folders-list): buscar "visible_to=me ..." → dice si es elevated_role (usuario no es operador), applied (con counts), o failed (con error).

### Logging (9c07b854a→4d6d00361→9c4ca2947)
- Historia: se agregaron logs → usuario dijo "logger no funciona" → pasé a console → confirmamos que Pino SÍ funciona (logs live Teams+User) → convertí a getLambdaLogger(event) (por-request, correlacionable) grants CRUD (create/update/delete/list) y roles CRUD (create/update/delete/clone).
- **GOTCHA:** el mock de @shared/observability/logger DEBE exponer createLogger Y getLambdaLogger — Utils.class usa createLogger al cargar; si el mock solo trae getLambdaLogger → "createLogger is not a function" en cualquier test que importe Utils.
- Campos logueados: SOLO no-sensibles (ids UUID, accountId, subject/object type, reason, counts). NUNCA bodies, roleName, emails, listas de permission_ids en logs operativos (el audit dedicado sí guarda permisos add/removed, es intencional).

### Logs en grant CRUD (daf7a36b6, antes había CERO)
createGrant/updateGrant/deleteGrant/listGrants: start / cada rejection (reason: object_not_found, subject_not_found, already_exists, not_found, account_mismatch) / success.

---

## BUGS/LECCIONES
1. **TDZ por import() dinámico concurrente:** N import() del mismo módulo con ciclo, en Promise.all, racean la init → "Cannot access X before initialization". Fix: carga memoizada secuencial una vez. Aplica a listOrgUsers y Teams.
2. **Fail-open en filtro de permisos = fuga.** Un filtro de visibilidad debe fallar-CERRADO ([]), nunca devolver todo.
3. **Pino filtra por nivel pero funciona** — "no veo logs" suele ser Lambda no redeployada o log group equivocado, no Pino roto.
4. **roleId inválido ("role-editor")**: la herencia ENCUENTRA el grant pero RoleModel.findById(roleId)=null → 0 permisos → 403. Para pruebas usar UUID de rol real.
5. **contador directo vs efectivo:** grant al padre no sumaba hijos → ahora expande por path.
6. **mock de logger incompleto** rompe todo test que importe Utils (createLogger).
7. Elevados (admin/superuser/superadmin) IGNORAN visible_to=me por diseño (PRD: admins ven todo). Probar filtro con Operador/Supervisor.

---

## PENDIENTE

### 1. Deploy (nada surte efecto sin esto)
- Infra CFN: Roles (GSI accountId-name-index en tabla role existente = 1 GSI/update, tabla RoleAuditLog, función clone, envs, roles IAM dedicados), Teams (envs FOLDERS_TABLE+CHATBOT_TABLE).
- Código Lambdas: user-list-org-users, teams-get-teams, folders-list, Access grants, Roles CRUD.
- Verificar que el build genere cloneMin.js.

### 2. Correr seed post-deploy
AWS_PROFILE=... ROLE_TABLE=dev-app-silia-com-role AWS_REGION=us-east-1 npx tsx scripts/migrations/seed-default-roles.ts
(pobla isDefault; de eso dependen las inmutabilidades)

### 3. Verificar post-deploy
- management: superusers con counts = totales (no 0).
- teams: contador refleja herencia.
- visible_to=me con operador: pegar la línea de log visible_to=me.
- logs grants/roles en su log group.

### 4. Decisiones/datos de producto (bloquean DATOS, no código)
- Matriz ~102 permisos validada celda-por-celda → habilita seed del mapeo rol→permiso (S2) y valor de category (S7).
- Discrepancia "Implementador" (PRD) vs enum código (superuser/superadmin) — seed usa DEFAULT_ROLES del código.
- Canal Slack para alerta >20 (hoy log).

### 5. Follow-ups (no bloquean demo)
- Feature 11 (asignación de roles custom) — S12 lo desbloquea, no construido.
- KMS Opción A para que delete cuente grants de AccessGrant (hoy fail-open).

### 6. PR
Abrir PR a develop con los reportes review+adversarial en la descripción.

---

## Review + Adversarial (final, tras mitigación Teams)
- PR Review: **Approved with suggestions ⚠️** — No issues. Suggestions: orden de deploy, fail-closed es cambio de comportamiento, verificar cloneMin.js.
- Adversarial 5 lentes: CORRECTNESS PASS, SECURITY PASS (mejora: fail-closed + cache scoped), INFRA PASS (1 GSI, sparse correcto, verificar clone build + GSI backfill), CONTRACT PASS (misma shape; rutas intactas), REVERSIBILITY PASS (aditivo). **Final: PASS ✅**, cero riesgos de código abiertos.
- tsc: limpio en archivos tocados. Errores de tsc del repo = pre-existentes (permission/*, resource/*, PermissionInvalidationService:203=FolderModel.findById original).

## Archivos clave
Roles/domain/models/{Role,Resource,RoleAuditLog}.model.ts · Roles/application/role/{create,update,delete,clone}.ts (+tests) · Roles/application/permission/getCatalog.ts · Roles/application/services/RoleAuditLogService.ts · Roles/infrastructure/{aws.template.yml,package.json} · scripts/migrations/seed-default-roles.ts · shared/config/permissions.ts (+test) · shared/middleware/requirePermission.ts · User/application/get/listOrgUsers.ts (+test) · User/domain/models/user.model.ts · Teams/application/get/getTeamsByAccount.ts (+test) · Teams/infrastructure/aws/aws.template.yml · Access/application/Grants/{post,patch,delete,get}/*.ts · Access/domain/services/PermissionInvalidationService.ts · Folders/application/Folders/get/listFolders.ts · docs/feature-10-roles-plan.md