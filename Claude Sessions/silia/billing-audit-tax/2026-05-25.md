---
tags: [claude-session, silia, billing, tax, security]
---

# Session: 2026-05-25/26 — Billing Audit Logs + Tax + Security Hardening

## Context
- **Branch:** feat/SL-682-audit-logs (merged), now on develop
- **Working directory:** /Users/sulli/Projects/silia/06-11-25/silia
- **Tickets:** SL-682 (Audit Logs), SL-1294 (US-TAX-01), SL-679 (Reactivate after payment)

## Work Log

### 1. Audit Log PR Review Fixes (SL-682)
- Fixed accountId: '' in reactivateAgent, retryPayment, setDefaultPaymentMethod, updateSubscription
- Added GSI index ARNs to BillingGeneralRole IAM
- Added PutItem on BillingAuditLog to BillingUploadProofRole
- Fixed reactivateAgent sentinel check (chatbot.id !== '0')
- User reverted some handler audit log integrations

### 2. US-TAX-01 — Stripe automatic_tax
- Finance decision (May 2026): Silia charges net USD, WHT is client's obligation
- Final: automatic_tax: { enabled: true } unconditionally in all 5 Stripe integration points
- Stripe returns /bin/zsh for non-US (no tax registrations), calculates Sales Tax for US
- Removed hardcoded MX=16% IVA, taxRate=0 everywhere
- Files: StripeGateway.ts (4 places), StripeWebhookHandler.ts (1), createSubscription.ts, Assistant/Put/index.ts (2)

### 3. RTA Sync — HTTP to DynamoDB (SL-679)
- Replaced HTTP-based syncRtaStatus in AccountStatusService with direct DynamoDB
- Created lightweight Accounts/domain/models/RtaConfig.model.ts (extends DB, findByAgentId + setStatus only)
- Can't import from RTA submodule (separate webpack build)
- Added RTA_CONFIGS_TABLE env var + IAM to Accounts template
- Backwards-compat fix: chatbot.suspensionReason ?? 'dunning' for pre-existing suspended chatbots

### 4. Security Hardening (Checkov)
- Created BillingAuditLogKmsKey (CMK) for BillingAuditLog table encryption (CKV_AWS_119)
- Cross-stack export of key ARN for Assistant template
- Created AssistantKmsKey (shared CMK for Lambda env + DynamoDB tables)
- Created AssistantApiDLQ (shared DLQ for API endpoint lambdas)
- Hardened 5 Lambda functions: GetWhatsAppTemplates, PatchChatbotConfig, GetIntents, PostIntent, DeleteIntent
  - ReservedConcurrentExecutions: 10 (CKV_AWS_115)
  - DeadLetterQueue → AssistantApiDLQ (CKV_AWS_116)
  - KmsKeyArn → AssistantKmsKey (CKV_AWS_173)
- Hardened 2 DynamoDB tables: IntentTable, AgentConfigAuditTable with CMK (CKV_AWS_119)
- Updated IAM: RoleChatbotRead + RoleChatbotWrite got kms + sqs permissions
- Removed BillingRateAuditTable from checkov baseline (replaced by CMK)

### 5. Checkov Baseline
- Multiple iterations: added entries, reverted, re-added
- Final: BillingRateAuditTable entry removed (table deleted + new table uses CMK)
- Pre-existing resources that were never baselined now have proper security controls

## Decisions Made
- automatic_tax always true (not conditional) — Stripe handles via tax registrations
- Lightweight cross-module model pattern for RTA config (no submodule import)
- CMK over baseline for new resources — following Metrics module pattern
- Shared KMS key for both Lambda env encryption and DynamoDB tables in Assistant
- ReservedConcurrentExecutions: 10 for API endpoints (conservative)
- DLQ on API lambdas even though synchronous (Checkov compliance)

## Current State
- **Branch:** develop (audit log + tax PRs merged)
- **All changes committed/merged**
- Only minor uncommitted files (settings, utils)

## How to Pick Up From Here
1. Suspension FE task — analysis saved in memory (project_suspension_fe_analysis.md)
2. AgentSuspensionService still uses HTTP for syncRtaStatus — separate task to migrate
3. FE tasks: US-TAX-02 (show tax_amount in Billing > Agents), US-TAX-03 (invoice detail)
4. Audit log read endpoint not yet built — IAM GSI permissions already added proactively