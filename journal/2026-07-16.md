# Journal — 2026-07-16

## SL-1289 — Feature 10 (roles custom) + fixes de demo
Sesión larga en rama fix/SL-1289-default-roles-migration (12 commits). Nota completa: [[Claude Sessions/silia/feature-10-roles/2026-07-16]].

### Hecho
- **Feature 10 backend completo** (12 subtareas): roles custom por cuenta (accountId nullable, isDefault, soft-delete, GSI accountId-name-index), CRUD con validaciones (unicidad 409, límite 100→422, permission_ids→422), clone "Copia de X", category en catálogo, audit dedicado (RoleAuditLog), resolución de permisos account-aware (S12, desbloquea Feature 11), invalidación de efectivos (sync/SQS >100), seed idempotente. ~27 tests.
- **Bug contadores management (0)**: era TDZ por import() dinámico concurrente en Promise.all → carga memoizada secuencial (loadEnrichDeps). El log WARN "Cannot access uh before initialization" lo delató.
- **Contadores efectivos** (con herencia) en management y teams — expandEffectiveCounts por prefijo de path, robusto a path vacío.
- **Visibilidad fail-closed** en listFolders (era fail-open → mostraba todo a operador = fuga de permisos).
- **Logging**: confirmamos que Pino SÍ funciona; migré grants+roles CRUD a getLambdaLogger(event). Gotcha: mock de logger debe traer createLogger Y getLambdaLogger (Utils lo usa al cargar).
- Teams: Folder/Chatbot via dynamic import memoizado (evita ciclo cold-start).
- Review + adversarial (scripts): PASS ✅.

### Decisiones
- PRD: catálogo permisos = global; roles custom = por cuenta (accountId nullable); aditivo sin big-bang; alcance = solo backend; rutas actuales /role /permissions/catalog; contadores = efectivos; feature flag coexistencia = N/A.

### Pendiente
- Deploy (infra+código) + correr seed + verificar + abrir PR.
- Producto: matriz ~102 permisos (habilita mapeo rol→permiso + category), "Implementador" vs enum, canal Slack alerta >20.
- Futuro: Feature 11 (asignación), KMS Opción A para conteo de grants en delete.

### Lecciones
- import() dinámico concurrente de módulos con ciclo → TDZ; usar carga memoizada.
- filtro de permisos SIEMPRE fail-closed.
- Pino funciona; "no veo logs" = Lambda no redeployada o log group equivocado.
- roleId inválido en grant → herencia lo encuentra pero resuelve 0 permisos → 403.