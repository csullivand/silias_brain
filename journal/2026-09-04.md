---
tags: [journal, silia]
date: 2026-09-04
---

# 2026-09-04

## Silia — Skill Assistant 7.2: deploy + investigación 9.2

Sesión: [[Claude Sessions/silia/Feature-7.2-skill-assistant-service/2026-09-04]]

### Cambios principales
- **7.2 desplegándose:** bump del puntero del submódulo Skills en Silia (`d856f8c → 6b9788e`, PR #9 mergeado upstream) → **PR #2292** contra develop. Rama `chore/SL-1637-bump-skills-pointer`, commit `2b51933b2` (1 file, solo gitlink).
- **Contract open item cerrado:** `edit_instructions.anchor` = `{ segmentIndex, start, end }` (no texto exacto), ya en `10a3e69`.
- **Reviews + CI replicado local:** todo verde salvo Checkov, que marca 2 tablas preexistentes de Alejandro (no nuestras).
- **Investigación Feature 9.2** escrita en `docs/feature-9.2-plan.md`.

### Decisiones
- Anchor por índice+offsets; backend emite `null` en anchor inválido.
- No tocar la deuda Checkov de Alejandro: además, un pointer-bump ni dispara iac-scan.
- Deploy por PR de bump de puntero (no commit directo a develop).

### Aprendizaje clave del día
- **Un pointer-bump de submódulo se ve como gitlink en el diff del superrepo**, así que los jobs de CI que filtran por ruta de archivo (iac-scan/Checkov, security-lint, runtime-consistency, secret-scan) **se saltan los internals del submódulo**. lint + tests (whole-tree) sí cubren Skills. ⇒ el CI de infra de Skills es un punto ciego (Skills no tiene CI propio).
- **9.2 no es construir un motor:** el motor (loop TS + submódulo Python `Agent`) ya existe con selección por intent, versionado y logging. Falta el PUENTE `ProcessFlow`→engine por intent. Boundary Skills↔Agent es lo que hay que cerrar con AGE.

### Pendiente
- Aprobar/mergear PR #2292 (dispara deploy).
- 9.2: llevar el boundary (§2 del plan) a AGE; cerrar 3 open questions del PRD.

## (cont.) 9.2 verificado + bloqueo de deploy

- **Feature 9.2 investigación VERIFICADA:** 2ª pasada adversarial (3 verificadores) → 12/12 claims cargantes CONFIRMED. La primera investigación estuvo bien. `docs/feature-9.2-plan.md` corregido (flow_id 3 términos, path del seed, nota de verificación).
- **Deploy de 7.2 BLOQUEADO por build preexistente:** webpack no puede bundlear el `.so` de sharp (cadena Chatbot.model → embeddings locales → transformers → sharp). No es 7.2; lo arregla otro equipo (externals en webpack.config.js). PR #2292 queda a la espera.
- Aprendizaje: el motor de ejecución de 9.2 ya existe (loop TS + engine Python); 9.2 es un puente/sync, no un motor nuevo. Boundary Skills↔Agent = decisión con AGE.