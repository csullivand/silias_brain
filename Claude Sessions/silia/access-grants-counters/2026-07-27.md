---
tags: [claude-session, silia, SL-1281, SL-1278, access, escalations]
---

# Session: 2026-07-23→27 — Counters, cross-account grants, role-editor, escalation-inbox

## Context
- **Repo:** /Users/sulli/Projects/silia/06-11-25/silia
- **Spanned:** SL-1281 (temas counters), SL-1278 (cross-account grants), role-editor investigation, escalation-inbox staging failure.

## PRs opened this session (all pending review/merge + redeploy)
| PR | Title | Service | Status |
|----|-------|---------|--------|
| #1700 | fix(user): count real grants for elevated roles in /user/management (SL-1281) | User | open |
| #1701 | fix(teams): count real folder/agent grants in /teams (SL-1281) + IAM + test | Teams | open |
| #1714 | fix(access): manage grants in a switched account (SL-1278) | Access + FE | open |

## 1. SL-1281 user counters (#1700)
- Bug: every superuser/admin on /user/management showed identical 23/92 (account total). Verified live on DEV (David Luna: /access/grants = 3 folders/2 agents, but list = 23/92).
- Root cause = MERGE REGRESSION: your #1692 (17:59) removed the elevated short-circuit in computeUserCounts; #1696 Implementador (19:45, stale branch) re-added it; 19:58 deploy shipped it.
- Fix: re-remove the short-circuit + drop ELEVATED_ROLES import + fix stale JSDoc (lines 217-227) that still said 'elevated see account totals'.

## 2. SL-1281 teams counters (#1701)
- Bug: EVERY team on /teams showed 0/0 even with grants (QA Escalations has 2 folders+2 agents). I was initially WRONG that teams had no grants — verified live they do.
- Root cause: fetchAccountResources used full ChatbotModel → imports Assistant tokenizer (gpt-3-encoder) NOT bundled in Teams Lambda → throws → swallowed fail-open → empty resources → #1692 phantom-fix drops every grant → 0/0.
- Fix: raw Assistant-free DynamoDB query (CHATBOT_TABLE/accountId GSI) for agents; Promise.allSettled so folders/agents independent.
- Adversarial-verify caught an INFRA BLOCK: TeamsGetByAccountRole lacked dynamodb:Query on Folders+Chatbot tables → would AccessDenied → still 0. Added IAM grant + real nonzero end-to-end test. See [[project_teams_lambda_chatbotmodel_bundling]].

## 3. role-editor investigation (no PR — diagnosis)
- Question: /access/grants returns role_id: 'role-editor' — what does it match?
- Finding: role-editor is a FRONTEND-only slug (AccessModal.tsx:20 DEFAULT_GRANT_ROLE_ID, ShareAccessModal options). Backend seeds roles with UUID ids (seed-default-roles → uuidv4), and editor/viewer are in ZERO backend files/matrix. So role-editor matches NOTHING → dangling grants → 0 resolved permissions.
- The exact save moment: git history — David's grants created 2026-07-15/16; the createGrant RoleModel.findById(roleId) validation only landed 2026-07-20 (Feature 5 #1594). Pre-Feature-5 createGrant.ts:188 saved roleId verbatim with NO role validation. So the FE-sent role-editor was accepted blindly then; today the same request 400s.

## 4. SL-1278 cross-account grants (#1714)
- Problem (user): a superuser in a SWITCHED account can't grant access (404/403).
- Verified mechanism: superuser 'enters' account B via URL only (/accounts/B/...); JWT is NEVER re-minted (AccountContext.tsx). getCallerContext().accountId = HOME account always.
- All 4 grant endpoints scoped to token account → broken cross-account.
- Fix: new shared helper resolveTargetAccount(event, requested) in requireRole.ts; createGrant+listGrants resolve+authorize a requested accountId (canAccessAccount); deleteGrant+updateGrant authorize vs the grant's OWN account. FE: accessService sends accountId (body/query), AccessModal passes its viewing accountId (already a prop, sourced from AccountContext.currentAccount = URL). 37 tests pass. No IAM change. See [[project_multi_account_jwt_home_account]].

## 5. escalation-inbox staging failure (deploy gap — no code change)
- URL: GET /chatbot/{id}/escalations/inbox on staging → 500.
- Lambda: staging-app-silia-com-escalation-inbox (SAM GetEscalationInbox, Assistant/application/Escalations/Lifecycle/Inbox), role RoleEscalationInboxList.
- Error: AccessDeniedException dynamodb:Query on staging-app-silia-com-role/index/accountId-name-index. Caller = supervisor (non-super) → assertPermission('agent.inbox.view') → RoleModel.findByAccountAndName → needs -role Query.
- Root cause: DEPLOY GAP. The CASL grant (role/permission/resource + indexes) IS in the template (aws.template.yml:1555-1564), added 2026-07-21 (b7f1d0795), and IS on the staging branch. But Assistant hasn't been redeployed to staging since → live IAM role is stale. Same class as [[project_casl_iam_grants_gap]].
- Fix: redeploy Assistant to staging: gh workflow run deploy-service.yml -f service_name=Assistant -f ref=staging -f code_ref=staging

## Current State
- 3 PRs open (#1700/#1701/#1714), all green, pending merge + redeploy of User/Teams/Access services.
- escalation-inbox needs Assistant→staging redeploy (no code change).

## How to Pick Up From Here
1. Merge #1700, #1701, #1714; redeploy User, Teams, Access.
2. Redeploy Assistant to STAGING to fix escalation-inbox (stale CASL IAM).
3. Re-verify live: David /user/management → 3/2; QA Escalations /teams → 2/2; escalation inbox 200 for supervisor.
4. (Optional) audit other non-super Assistant Lambdas on staging for the same stale-IAM assertPermission wall.
5. (Optional) fix FE role-editor: send a real role id or seed editor/viewer object-ACL roles (SL-1278 TODO AccessModal.tsx:18).