---
tags: [journal, silia]
---

# 2026-07-23

## Main work: Implementador role (SL-1272, PR #1696)
Added `implementador` as a distinct, matrix-driven default role instead of the old conflation with god-mode `superuser`. Full arc: enum + DEFAULT_ROLES + role-permissions.json (99/102 cells, verified cell-by-cell vs PRD across all 5 roles), ~38 handler guards, seed scripts (B1/B3/B4) + contract test, frontend gates. Cleaned up 12 `'superadmin' as Role` casts.

## Decisions
- `implementador` = `superuser` minus `platform.implementers.*`; `superuser` retained as internal bypass.
- **IMPLEMENTADOR kept OUT of ELEVATED_ROLES** — that constant gates privilege-escalation surfaces (register.ts, assignAccounts, revokeAccounts, legacy update.ts). Capabilities granted via explicit per-handler allowlists instead.
- Voice/Integrations excluded (not in PRD).

## Review cycle — 3 real BLOCKs fixed + 1 build break + 1 mistake
- ELEVATED_ROLES escalation (cross-tenant superadmin mint) → removed from constant.
- Legacy update.ts (no permission/allowlist/tenant guard) → removed implementador.
- GetChannels/PutChannels 403 gap → added implementador (came in via develop merge).
- Build break: develop merge deleted the ELEVATED_ROLES import in listOrgUsers.ts → restored.
- **Mistake:** initially called GetChannels/PutChannels a hallucination; they were real (moved in via merge). Lesson: re-verify surprises against current HEAD (git ls-files + tsc), branch moves under develop merges.

## Pending
- Run seeds per env (manual): B1→B2→B3→B4.
- Reassign existing implementer users superuser→implementador.
- Follow-up ticket: harden/retire update.ts (pre-existing ADMIN escalation).

Related: [[Claude Sessions/silia/implementador-role/2026-07-23]]