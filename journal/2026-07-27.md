---
tags: [journal, silia]
---

# 2026-07-27 — Access grants, counters, escalation-inbox

Big multi-topic session on [[Claude Sessions/silia/access-grants-counters/2026-07-27|silia]]. Three PRs opened + one staging deploy-gap diagnosed.

## Shipped (PRs, pending merge)
- **#1700** — /user/management elevated roles now count real grants (fixes a MERGE regression: #1696 re-introduced the short-circuit #1692 removed).
- **#1701** — /teams counts real grants; root cause was ChatbotModel's tokenizer not being bundled in the Teams Lambda (fail-open → 0). Added the missing Folders/Chatbot IAM grant after adversarial-verify flagged it.
- **#1714** — cross-account grant management (SL-1278). JWT is never re-minted on account switch, so all 4 grant endpoints were stuck on the home account.

## Diagnosed (no code / deploy only)
- **role-editor** is a frontend-only slug that matches nothing in the backend role catalog → dangling grants; they were saved before Feature 5 (#1594) added roleId validation.
- **escalation-inbox 500 on staging** = stale IAM. The CASL grant is in the template + staging branch (since 21-jul, b7f1d0795) but Assistant hasn't been redeployed to staging → .

## Decisions / lessons
- Bundling is per-Lambda: a model that works in one service can 500 in a leaner one ([[project_teams_lambda_chatbotmodel_bundling]]).
- Multi-account: never trust token.accountId as the target account ([[project_multi_account_jwt_home_account]]).
- 'Merged' ≠ 'deployed', and a stale branch can silently revert a merged fix.

## Pending
- Merge the 3 PRs + redeploy User/Teams/Access; redeploy Assistant→staging; re-verify live.