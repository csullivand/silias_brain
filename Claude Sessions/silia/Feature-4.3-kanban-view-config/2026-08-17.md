---
tags: [claude-session, silia, kanban, feature-4-3, backend]
---

# Session: 2026-08-17 — [Feature 4.3][BE] Persistencia de configuración de vista Kanban por usuario

## Context
- **Branch:** feat/SL-1576-persistencia-reglas-fill (current; Feature 4.3 no branch yet)
- **Working directory:** /Users/sulli/Projects/silia/06-11-25/silia
- **Ticket/Issue:** [Feature 4.3][BE] — Persistencia de la configuración de vista por usuario (Kanban). PRD: Vista Kanban para Tablas (Data Views)
- **Goal for this session:** Analizar el ticket, ubicar el patrón espejo (SL-1576 Row Fill / FilterBarConfig / KpiPrefs / UserViewConfig), y producir un plan de implementación del store per-(userId,tableId) + endpoints GET/PUT + cascade-delete.

## Work Log (step by step)

### Step 1: Análisis del ticket + doc FE congelado
- **What:** Leído docs/kanban-view-config-api.md (contrato FE congelado, untracked). Lanzado agente Explore para mapear stores per-user existentes en DynamicTables.
- **Why:** Feature 4.3 es hermano de SL-1576 (Feature 2.1 Row Fill) — mismo patrón per-(userId,tableId).
- **Files touched:** ninguno (análisis)
- **Result:** en progreso.

## Decisions Made
- (pendiente)

## Problems / Errors Encountered
- Discrepancia de contrato: doc FE dice 422 (validación) y updatedAt en ms; SL-1576 aterrizó 400+errorCode y updatedAt en segundos. Alinear antes de codificar.

## Current State (last save)
- **Branch status:** sin rama Feature 4.3 aún; en feat/SL-1576
- **What's working:** SL-1576 (Feature 2.1) ya mergeable como espejo
- **What's broken/incomplete:** Feature 4.3 no desarrollado

## How to Pick Up From Here
1. Revisar salida del agente Explore (mapa de stores + cascade-delete en deleteTable).
2. Decidir contrato final (422 vs 400+errorCode; updatedAt ms vs seg).
3. Crear rama y espejar el store rowFillRules/FilterBarConfig para kanban-view-config.



---

### Step 2: Análisis completo entregado (checkpoint auto-save)
- **What:** Mapeados los 4 stores per-(userId,tableId) existentes (RowFillRules, KpiPrefs, FilterBarConfig, UserViewConfig) vía agente Explore. Confirmadas convenciones reales leyendo putRowFillRules.ts, putKpiPrefs.ts, los 3 modelos y Utils.nowTimeStamp.
- **Why:** Feature 4.3 = clon del patrón. Necesitaba contrato real vs doc FE congelado.
- **Files touched:** ninguno de código (solo lectura + notas Obsidian)
- **Result:** Análisis entregado al usuario. Conclusiones:

**Patrón (molde a copiar = SL-1576 RowFillRules):**
- Modelo PK=userId, SK=tableId, GSI tableId-index. deleteByTableId query GSI.
- Handlers GET/PUT dedicados: /tables/{tableId}/kanban-view-config
- Cascade en application/Tables/delete/deleteTable.ts:44-57 (fail-open, +1 línea)
- Infra: tabla+GSI+KMS+rol DtKanbanViewConfigRole (espejo DtRowFillRulesRole); deleteTable role necesita Query+DeleteItem sobre tabla+GSI.

**⚠️ HALLAZGO CRÍTICO — doc FE congelado (docs/kanban-view-config-api.md) contradice el código real:**
| Aspecto | Doc FE | Código real |
| updatedAt | ms (1723526400000) | SEGUNDOS (Utils.nowTimeStamp = floor(Date.now()/1000)) en los 3 stores |
| Error validación | 422 + {message} | RowFill 400+{errorCode,message,rule}; Kpi 422+{message,error} |
| Permiso PUT | table.view | RowFill/FilterBar usan table.filter; solo Kpi usa table.view |
| GET sin config | {config:null, updatedAt:null} | RowFill/Kpi devuelven arrays vacíos (para blob objeto, null es razonable) |
| Éxito | envuelto {config, updatedAt} | RowFill/Kpi desenvuelto |

- Recomendación: alinear BE a convención de casa (seg, table.filter) + actualizar doc FE. PERO doc dice 'congelado' y FE ya avanzó → DECISIÓN de producto/usuario, no asumir.
- Edge case reconciliación: ticket+doc dicen BE guarda VERBATIM (no dropea columnas borradas, dueño FE). Difiere de RowFill que sí dropea. Confirmar.
- summaryAggs solo se persiste aquí (cálculo es Feature 8).

## Decisions Made
- (BLOQUEADO) Contrato final: ms/422/table.view (doc) vs seg/400/table.filter (casa). Pendiente decisión usuario.

## Current State (last save)
- **Branch status:** sin rama Feature 4.3; git sin cambios de código (solo análisis)
- **What's working:** análisis completo; plan de implementación listo
- **What's broken/incomplete:** implementación no iniciada; contrato sin decidir

## How to Pick Up From Here
1. Resolver contrato (sección 2 del análisis): ¿BE se pliega al doc congelado o actualizamos doc a convención de casa?
2. Crear rama; espejar RowFillRules → KanbanViewConfig (modelo, util validador de enums direction/calcs/__no_status__ por id, handlers GET/PUT).
3. Infra SAM: tabla+GSI+KMS+rol; +1 línea cascade en deleteTable.ts.
4. Tests espejo de RowFillRules.


---

### Step 3: Gap descubierto — mover/arreglar posición de cards
- **What:** Usuario señaló que se pueden mover cards y arreglar posición. Analizado el gap en el contrato congelado.
- **Result:** Dos operaciones distintas:
  1. **Mover card ENTRE lanes** (Pending→Confirmed) = NO es view config; muta el valor de la columna group_by en la fila = DATA COMPARTIDA vía endpoint existente de row/cell update. Feature 4.3 no lo toca.
  2. **Arreglar posición DENTRO de una lane** (drag manual, no sort por columna) = GAP. El contrato solo tiene laneSorts (sort por columna+dirección). NO hay campo para orden manual.

- **Falta en contrato:** o (a) modo 'manual' + laneManualOrder: {optionId → [rowId,...]}, o (b) rank/position en la fila (compartido).
- **⚠️ 2 decisiones de producto (no asumir):**
  1. ¿Orden manual es per-user o COMPARTIDO? Kanban típico (Trello/Jira) = orden compartido del board → va en la FILA (rank column), NO en este blob per-user.
  2. ⚠️ ESCALA: si el orden manual va per-user en el blob → crece O(nº filas) (un rowId por card por lane por user). Todo lo demás es O(columnas/opciones), acotado. Miles de filas → revienta límite item DynamoDB 400KB + cada PUT reescribe la lista completa.
- **Recomendación:** manual within-lane = orden COMPARTIDO → rank/position en la fila (fractional/lexo-rank: un move = una escritura barata, sin reindexar). Mantiene el store per-user pequeño. Si producto exige per-user → añadir laneManualOrder al contrato + cap + marcar riesgo de tamaño.

## Decisions Made (actualizado)
- (BLOQUEADO x2) 1) Contrato ms/422/table.view vs seg/400/table.filter. 2) Orden manual de cards: per-user (blob) vs compartido (rank en fila). Recomendado: compartido/rank.

## How to Pick Up From Here (actualizado)
1. Resolver 2 decisiones de contrato: (a) convención casa vs doc; (b) card ordering per-user vs rank compartido en fila.
2. Confirmar que mover-entre-lanes usa el endpoint de row-update existente (no 4.3).
3. Espejar RowFillRules → KanbanViewConfig; infra SAM + cascade; tests.


---

### Step 4: DECISIONES CERRADAS — pivote a compartida por tabla
- **What:** Usuario consultó con producto. Revisado el PRD (Vista Kanban para Tablas).
- **Decisiones finales:**
  1. **Kanban config = COMPARTIDA por tabla** (NO per-user). Clave solo tableId, sin userId. El usuario dijo: 'el PRD está mal, ya se va a corregir'. (El PRD hoy dice por-usuario en múltiples lugares + decisión fechada 14-jul-2026, PERO queda invalidado por la consulta.)
  2. **Orden manual de tarjetas = NO persiste** (confirmado por PRD: fuera de alcance; business rule 'nunca posición manual persistida'; Feature 7 es sort automático client-side). Sin rank, sin follow-up.
  3. **Contrato = convención de casa (SL-1576):** updatedAt segundos, error 400+{errorCode,message}, GET sin config → {config:null, updatedAt:null}.
  4. **Permiso PUT:** al ser compartida ya no es table.view. PRD: configurar vista = permiso de edición (como editar celdas/columnas). Análogo en código: reorderColumns usa 'table.column.edit'. RECOMENDADO GET=table.view, PUT=table.column.edit (pendiente confirmar mapeo).

- **Simplificación vs SL-1576:** sin GSI tableId-index (no hay barrido per-user); cascade trivial (un item por tabla).
- **Docs a corregir (no-código):** ticket + PRD (por-usuario→compartida); docs/kanban-view-config-api.md (quitar aislamiento userId, alinear seg/400).

## Decisions Made (FINAL)
- Compartida por tabla · sin rank · convención casa (seg/400) · GET table.view / PUT table.column.edit (confirmar).

## How to Pick Up From Here (FINAL)
1. (Opcional) Confirmar permiso PUT = table.column.edit.
2. Implementar: modelo KanbanViewConfig (PK=tableId, sin userId/GSI), util validador de enums (direction/calcs/__no_status__), handlers GET/PUT, cascade en deleteTable.ts, infra SAM (tabla+KMS+rol, SIN GSI), tests.
3. Actualizar docs/kanban-view-config-api.md.


---

### Step 5: Implementación completa + reviews + commit (2026-08-19)
- **What:** Espejé SL-1576 RowFillRules → Kanban view config (compartida por tabla). Corrí PR review + adversarial verify. Committeé solo DynamicTables (sin docs).
- **Rama:** feat/SL-1477-persistencia-vista-usuario-kanban (ticket SL-1477).
- **Commit:** a079d4d0e — feat(eca): persist shared Kanban view config per table (SL-1477). 11 archivos, +924. Hooks OK (security lint, dev-rules sync, commitlint). SIN push.

**Archivos (en el commit):**
- domain/models/KanbanViewConfig.model.ts (PK=tableId, sin userId/GSI; findByTableId con findOne, save, deleteByTableId)
- domain/utils/kanbanViewConfig.ts (valida solo enums direction/calcs + forma; guarda VERBATIM; error 400+errorCode)
- application/KanbanViewConfig/get|put/*.ts (GET table.view, PUT table.column.edit; updatedAt segundos; GET sin config → {config:null,updatedAt:null})
- domain/types/tables.types.ts (+tipos/enums KanbanViewConfig, KANBAN_CALCS, KANBAN_SORT_DIRECTIONS, KANBAN_NO_STATUS_KEY, KanbanViewConfigI)
- application/Tables/delete/deleteTable.ts (+1 cascada, fail-open)
- infrastructure/aws.template.yml (KMS key/alias, tabla sin GSI, env var KANBAN_VIEW_CONFIG_TABLE, DtKanbanViewConfigRole con CASL+AccessGrant+KMS, 2 Lambdas, RequestModel, recurso en delete-role)
- 3 suites de test → 32 tests verdes (incl. model validation test).

**Reviews:**
- Adversarial verify (5 lentes): PASS ✅.
- PR review: Approved with suggestions ⚠️.
- Ambos convergieron en 1 hallazgo: updatedBy sin @IsOptional() → DB.save valida con skipMissingProperties:false → save omite escritura en silencio devolviendo 200 (no alcanzable en prod, authorizer siempre pobla userId). FIX aplicado (@IsOptional + import) + test de regresión (KanbanViewConfig.model.test.ts).
- Nota menor no bloqueante: handler PUT ignora retorno de save() (igual que RowFillRules/KpiPrefs; se deja por consistencia).

**FUERA del commit (working tree):** docs/kanban-view-config-api.md (contrato FE actualizado a: compartida / segundos / 400 / table.column.edit + changelog). Otros untracked ajenos (escalacion-*, kpi-* docs, updateColumn test, .changeset/free-lizards-crash.md).

## Current State (last save) — FINAL
- **Branch:** feat/SL-1477-persistencia-vista-usuario-kanban · commit a079d4d0e (local, sin push)
- **What's working:** feature completa, 32 tests verdes, tsc limpio (fuentes), reviews PASS, commiteado
- **What's broken/incomplete:** NO deployado (falta sam deploy + round-trip real); doc FE sin commitear (por decisión); PRD/ticket aún dicen 'por-usuario' (a corregir por producto)

## How to Pick Up From Here — FINAL
1. (Opcional) git push de la rama + abrir PR (descripción no técnica ya redactada).
2. sam deploy a dev + round-trip real (tabla nueva se crea con KMS, sin GSI).
3. Decidir si el doc FE se commitea aparte o se trackea fuera del PR (como se hizo en SL-1576).
4. Corregir PRD/ticket: 'por-usuario' → 'compartida por tabla'.