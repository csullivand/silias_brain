---
tags: [claude-session, silia, billing, metering]
---

# Session: 2026-05-04 — SL-1146 Metering Minutes

## Context
- **Branch:** feat/SL-1146-metering-minutes
- **Working directory:** /Users/sulli/Projects/silia/06-11-25/silia
- **Ticket/Issue:** SL-1146 + new metering ticket (batch job + eventos)
- **Goal for this session:** Complete infra for BillingRateAudit IAM + implement usage tracking on call end

## Work Log (step by step)

### Step 1: Add IAM permissions for BillingRateAudit table
- **What:** Added dynamodb:PutItem and dynamodb:Query permissions to BillingGeneralRole for the BillingRateAudit table
- **Why:** createSubscription handler needs to write audit records when per-minute rates are configured
- **Files touched:** Billing/infrastructure/aws.template.yml (line ~316)
- **Result:** BillingGeneralRole now has PutItem+Query on BillingRateAudit table

### Step 2: Research RTA call end flow
- **What:** Investigated how AudioStream handles call lifecycle (start → sessionEnd → disconnect)
- **Why:** Need to understand where to hook usage tracking
- **Key findings:**
  - AudioStream (Assistant/application/AudioStream/index.ts) manages voice sessions via Socket.IO
  - `startCallFlow` event initiates the call, `disconnect` / `sessionEnd` ends it
  - `handleSessionCleanup()` is the reliable cleanup point (fires on any disconnect)
  - chatbot.accountId available from initial chatbot lookup
  - No existing duration tracking — placeholder `minutesUsed = 0` in transformers.ts:161

### Step 3: Create BillingUsage model
- **What:** Created Billing/domain/models/BillingUsage.model.ts
- **Why:** Store per-call usage records for later reporting to Stripe
- **Fields:** id, chatbotId, accountId, sessionId, agentType, unit, durationSeconds, minutes, reportedToStripe, stripeUsageRecordId, timestamp
- **Files touched:** Billing/domain/models/BillingUsage.model.ts (NEW)

### Step 4: Implement usage tracking in AudioStream
- **What:** Extended AudioSession interface + record callStartTime on startCallFlow + write BillingUsage on cleanup
- **Why:** Track actual call duration server-side (reliable even if client crashes)
- **Files touched:** Assistant/application/AudioStream/index.ts
- **Details:**
  - Added `chatbotId`, `accountId`, `callStartTime` to AudioSession interface
  - Set callStartTime = Date.now() after setupCompleteVoiceInteraction succeeds
  - In handleSessionCleanup: calculate duration, Math.ceil to minutes, save BillingUsageModel
  - SIGINT handler now uses handleSessionCleanup (ensures usage recorded on graceful shutdown)

### Step 5: Infrastructure — DynamoDB table + IAM + env vars
- **What:** Added BillingUsage DynamoDB table with GSI, env vars, and IAM permissions
- **Files touched:**
  - Billing/infrastructure/aws.template.yml: BillingUsageTable resource + ChatbotIdTimestampIndex GSI + BILLING_USAGE_TABLE env var + IAM for BillingGeneralRole
  - Assistant/infrastructure/aws.template.yml: BILLING_USAGE_TABLE env var + IAM PutItem for RoleAudioStream

### Step 6: Analyzed remaining ticket work
- **What:** Compared implemented vs remaining requirements, drafted implementation plan
- **Remaining:**
  1. Batch job Lambda (daily → report usage to Stripe via usage_record.create)
  2. Wire minutesUsed in frontend transformer (replace placeholder)
  3. Retry/alerting for batch failures

## Files Modified This Session
| File | What changed | Why |
|------|-------------|-----|
| Billing/infrastructure/aws.template.yml | DynamoDB tables (BillingRateAudit IAM + BillingUsage table + GSI + IAM) | Infra for usage tracking |
| Billing/domain/models/BillingUsage.model.ts | NEW — usage record model | Store per-call usage |
| Assistant/application/AudioStream/index.ts | Duration tracking + usage write on cleanup | Core usage recording |
| Assistant/infrastructure/aws.template.yml | BILLING_USAGE_TABLE env var + IAM for RoleAudioStream | AudioStream needs DynamoDB access |

## Decisions Made
- Rounding: Math.ceil (round up to nearest minute) — industry standard
- No internal HTTP endpoint needed — AudioStream writes directly to DynamoDB (simpler, more reliable)
- No cross-team event schema needed — duration calculated server-side
- Batch job should use `action: 'increment'` per daily window with `reportedToStripe` flag for safety

## Current State (last save)
- **Branch status:** 13 modified + 2 new files, 303 insertions, all uncommitted
- **What's working:** Full usage tracking pipeline from call start → call end → DynamoDB
- **What's incomplete:** Batch job to Stripe, minutesUsed transformer, retry/alerting

## How to Pick Up From Here
1. Implement batch job Lambda: Billing/infrastructure/aws/handlers/reportUsageToStripe/
2. Wire minutesUsed in Assistant/application/Get/transformers.ts (replace placeholder at line 161)
3. Add retry (DLQ) + alerting to batch job
4. Commit all changes
5. Test end-to-end: make RTA call → verify BillingUsage record → run batch → verify Stripe