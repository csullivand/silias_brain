# 2026-08-05

## Silia — SL-1528 Feature 6: endpoint de agregaciones KPI

### Cambios principales
- Rama **`feat/SL-1528-agregaciones-dataset-kpis`**, commit `943b6902b`: `POST /tables/{tableId}/aggregations` — calcula los valores de las KPI cards sobre el set filtrado completo. Resuelve "las cards con valor vacío" de Camila (FE).
- Service reusa `paginateFilteredRows` + `computeAggregation` del table assistant + `filterBuilder`; mapea los 13 kinds. 17 tests verdes. pr-review + adversarial = Approved/PASS.
- Docs FE creados (sin commitear): `kpi-aggregations-frontend-integration.md` (ES) + `kpi-prefs-frontend-integration.md`.

### Decisiones
- DynamoDB (no SQL) -> agregación in-memory con soft cap `KPI_AGG_MAX_ROWS` (default 50000) -> `partial:true`.
- Volumen: best-effort + soft cap (no cap duro / no sin cap). Parsing: reusar `toFiniteNumber`.

### Aprendizajes / hallazgos
- **develop no despliega KpiPrefs**: el merge de Feature 5.1 perdió los build entries; falta hotfix.
- Merge de develop trabado por conflictos ya resueltos (env + CSP QA assistant) -> concluido.

### Pendiente
- Confirmar límite de cards **6 vs 4** (código/PRD dicen 6).
- Hotfix build entries KpiPrefs a develop.
- Push + PR de SL-1528.

Relacionado: [[Claude Sessions/silia/SL-1528-kpi-aggregations/2026-08-05]] · [[Claude Sessions/silia/SL-1526-kpi-prefs-persistence/2026-08-04]]