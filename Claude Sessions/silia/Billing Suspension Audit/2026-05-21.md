---
tags: [claude-session, silia, billing, suspension, dunning]
---

# Session: 2026-05-21 — Billing Suspension Feature Audit

## Context
- **Branch:** develop
- **Working directory:** /Users/sulli/Projects/silia/06-11-25/silia
- **Ticket/Issue:** BILLING: FE: Suspensión automática por falta de pago
- **Goal for this session:** Audit codebase against acceptance criteria to map what exists and what needs to be built

## Work Log (step by step)

### Step 1: Deep codebase audit — Backend
- **What:** Searched entire backend for dunning, suspension, account status, agent deactivation, email notifications, billing module structure, and reactivation logic
- **Why:** Need to map every acceptance criterion to existing code
- **Key findings:**
  - `Billing/application/handlers/StripeWebhookHandler.ts` — Full dunning flow: `DUNNING_WARNING_THRESHOLD = 7`, `DUNNING_MAX_ATTEMPTS = 8`. Handles `invoice.payment_failed`, subscription status changes, and invoice uncollectible
  - `Billing/application/services/AgentSuspensionService.ts` — Per-agent suspension (RTA agents): sets status=0, suspensionReason='dunning', pauses Stripe subscription, syncs RTA service
  - `Accounts/domain/services/AccountStatusService.ts` — Account-level suspension: changes status active↔inactive, bulk deactivates all chatbots, pauses all Stripe subscriptions, logs to AccountStatusAudit
  - `Billing/application/services/BillingService.ts:138-217` — Safety-net cron job (`deactivateOverdueSubscriptions()`) with `SAFETY_NET_DAYS = 9`, runs daily at 2:00 AM UTC
  - `Billing/application/services/NotificationService.ts` — Full email suite: dunning escalation, pre-suspension warning, account suspended, agent dunning suspended, commercial team alerts
  - `StripeWebhookHandler.activateAssistant()` lines 477-577 — Automatic reactivation on payment: detects invoice.paid, reactivates chatbots, reactivates account if was inactive
  - `POST /agents/{chatbotId}/reactivate` — Manual reactivation, superuser role only
  - Account status values: only `'active'` | `'inactive'` (no explicit 'suspended' state)
  - `suspensionReason` field on Chatbot model: `'dunning' | 'manual' | null`
  - Audit table: `AccountStatusAudit` with previousStatus, newStatus, timestamp, performedByUserId

### Step 2: Deep codebase audit — Frontend
- **What:** Searched frontend for account status handling, route protection, global banners, billing UI, agent management
- **Why:** Need to know what frontend pieces exist for suspension UX
- **Key findings:**
  - `app/src/features/auth/components/ProtectedRoute.tsx` — Route guard checks auth + role permissions, but NOT account status
  - `app/src/features/account/components/AccountManagement/AccountManagement.tsx` — Shows agent list with dunning status, disables toggle for dunning-suspended agents with tooltip
  - `app/src/features/account/utils/assistantBillingUtils.ts` — Has `SuspensionReason` enum, checks `isDunning` condition
  - `app/src/Components/layouts/MainLayout.tsx` — Main layout, only has InactivityWarningModal, no suspension banner
  - `app/src/features/modules/components/Modules.tsx` — Module enable/disable UI, no account-status gating
  - Note: codebase has typo 'innactive' in some places

### Step 3: Gap analysis
- **What:** Created matrix mapping each acceptance criterion to existing code or identified gap
- **Result:** See Decisions Made section

## Files Modified This Session
| File | What changed | Why |
|------|-------------|-----|
| No files modified | Research/audit only | — |

## Decisions Made
- Account uses `inactive` status (not a new 'suspended' state) — ticket confirms "Solo hay 2 estados activo e inactivo - Ya se hizo"
- Identified 4 major gaps to build:
  1. **Frontend: Banner global de cuenta suspendida** — No banner exists in MainLayout
  2. **Frontend: Control de acceso a módulos por estado** — ProtectedRoute doesn't check account.status, no selective module blocking
  3. **Backend: Timeline 30 días de gracia** — No `suspendedAt` field, no scheduled job for data retention policy
  4. **Frontend: Modo read-only para cuentas suspendidas** — No conditional disable of edit actions

## Problems / Errors Encountered
- None

## Current State (last save)
- **Branch status:** develop, no new changes (pre-existing unstaged changes in csvProcessor, fileValidator, listTables)
- **What's working:** Full backend dunning→suspension→reactivation pipeline, email notifications, audit logging, agent-level dunning UI in AccountManagement
- **What's broken/incomplete:** No global suspension banner, no module access control by account status, no 30-day grace timeline, no read-only mode

## How to Pick Up From Here
1. Decide priority order for the 4 gaps (banner is highest impact, most visible)
2. For banner: modify `app/src/Components/layouts/MainLayout.tsx` to check `account.status` from AccountContext and render persistent Ant Design Alert
3. For module access: extend `ProtectedRoute.tsx` or create `SuspendedAccountGuard` that reads account status and redirects/blocks based on allowed modules list
4. For 30-day grace: add `suspendedAt` field to Account model, create scheduled Lambda similar to `deactivateOverdueSubscriptions`
5. For read-only: create hook `useIsSuspended()` that components can use to conditionally disable edit actions