---
tags: [claude-session, silia, billing]
---

# Session: 2026-05-25 — Billing Audit Logs + Tax Conditional

## Context
- **Branch:** feat/SL-682-audit-logs
- **Working directory:** /Users/sulli/Projects/silia/06-11-25/silia
- **Ticket/Issue:** SL-682 (Audit Logs), SL-1294 (US-TAX-01)
- **Goal for this session:** Finish audit log PR review fixes + implement conditional automatic_tax (US only)

## Work Log

### Checkpoint 1: Audit Log PR Review Fixes
- Fixed accountId: '' in reactivateAgent (fetch chatbot → chatbot.accountId)
- Fixed accountId: '' in retryPayment (invoice.customerId → AccountModel.findByStripeCustomerId)
- Fixed accountId: '' in setDefaultPaymentMethod (body.customerId → AccountModel.findByStripeCustomerId)
- Fixed updateSubscription using Stripe customerId instead of internal accountId
- Added GSI index ARNs to BillingGeneralRole IAM for future queries
- Added PutItem on BillingAuditLog to BillingUploadProofRole
- User reverted audit log changes from handlers (linter removed imports)

### Checkpoint 2: US-TAX-01 — Conditional automatic_tax
- **Decision:** country=US → automatic_tax enabled (Stripe calculates Sales Tax per state). All others → disabled, tax=/bin/zsh
- **StripeGateway.ts:** All 4 places now use `country === 'US'` from metadata
  - createSubscription (line 313): `automatic_tax: { enabled: country === 'US' }`
  - createScheduledSubscription (line 389): same
  - updateSubscription schedule path (line 476): reads `scheduleCountry` from phase metadata
  - updateSubscription regular path (line 501): retrieves sub, reads `subCountry` from metadata
- **StripeWebhookHandler.ts (line 706):** setup_intent.succeeded reads `phaseCountry` from schedule metadata
- **createSubscription.ts:** taxRate = 0 (was MX ? 16 : 0)
- **Assistant/Put/index.ts:** Both taxRate assignments changed to 0 (lines 525, 683)
- Removed all hardcoded MX=16% tax logic

## Files Modified This Session
| File | What changed | Why |
|------|-------------|-----|
| Billing/infrastructure/gateways/StripeGateway.ts | automatic_tax conditional on country=US, refactored updateSubscription to retrieve sub once | US-TAX-01 |
| Billing/application/handlers/StripeWebhookHandler.ts | phaseCountry check for automatic_tax | US-TAX-01 |
| Billing/infrastructure/aws/handlers/createSubscription/createSubscription.ts | taxRate=0 instead of MX?16:0 | US-TAX-01 |
| Assistant/application/Put/index.ts | taxRate=0 in both places | US-TAX-01 |

## Decisions Made
- Tax for non-US: always /bin/zsh. WHT is client's contractual obligation.
- Tax for US: Stripe Tax auto-calculates. Initial taxRate=0 on chatbot, real value updated after invoice finalization.
- No wht_config table needed. No WHT notes on invoices.
- updateSubscription refactored: retrieves subscription once at top (needed for country check anyway)

## Current State
- **Branch status:** 8 files modified, not committed
- **What's working:** All automatic_tax conditionals in place, taxRate logic simplified
- **What's broken/incomplete:** Audit log handler imports were reverted by user/linter — need to verify final state

## How to Pick Up From Here
1. Verify audit log changes — user may have intentionally stripped them from some handlers
2. Test: create subscription with country=US → verify automatic_tax:true in Stripe
3. Test: create subscription with country=MX → verify automatic_tax:false
4. Check if Stripe Tax is configured in the Stripe dashboard (required for automatic_tax to work)
5. FE tasks: US-TAX-02 (show tax_amount in Billing > Agents), US-TAX-03 (invoice detail)