---
tags: [claude-session, silia, feature-5, access-grants, access, permissions]
---

# Session: 2026-07-15 — [Feature 5] access_grants polimórfico (review + implementación)

## Context
- **Branch:** fix/SL-1282-access-polimorficos (renombrada por el usuario desde feat/SL-0-access-grants-f5; basada en develop)
- **Working dir:** /Users/sulli/Projects/silia/06-11-25/silia/
- **Ticket:** SL-1282 / [Feature 5] [BE] Modelo de datos y endpoints de access_grants polimórficos
- **Objetivo:** revisar qué faltaba de la historia Feature 5 (modelo AccessGrant + endpoints /access) e implementar los gaps.
- **Estado global:** desarrollo COMPLETO (todos los AC + soft delete + admin_objects). SIN COMMITEAR. Falta commit/push/PR/seed/deploy/verify.

## Revisión inicial (qué existía)
Ya existía el módulo Access: modelo AccessGrant (id, accountId, subjectType user|team, subjectId, objectType folder|agent|table, objectId, roleId, grantedBy, createdAt, updatedAt) + endpoints POST/GET/PATCH/DELETE /access/grants + GET /access/effective + invalidación de cache (PermissionCache) + processor SQS (Feature 6). Gaps: idempotencia, validación role, permiso share, audit, herencia, cascada, invalidación team, dashboard, soft delete, admin_objects.

## Decisiones de producto (con el usuario)
- **D1 — ¿qué roles pueden compartir (share)?** → **SOLO ADMIN**. La historia (AC-6 + Necesidades técnicas) pide el permiso [object].share_access pero NO dice qué roles lo tienen. admin/superuser/superadmin bypassan igual. Seed concede folder.share/agent.share/table.share solo a admin. supervisor/operator = decisión futura.
- **D2 — ¿activar PERMISSION_STRICT_MODE?** → **NO, dejar apagado**. El sistema object-level corre en modo permisivo (falla → advierte y deja pasar, no 403). El flag es GLOBAL (afecta todos los endpoints object-level), así que prenderlo es rollout coordinado aparte (dev→staging→prod). Consecuencia: hoy el check share NO bloquea de verdad a no-admins, solo advierte.

## Subtareas IMPLEMENTADAS (todas con tests, tsc limpio en archivos tocados)

### Prioridad alta
- **F5-S1 idempotencia** (AC-2): createGrant.ts — duplicado con MISMO role → 200 con el existente; role distinto → 409.
- **F5-S2 validar role** (edge case → 400): createGrant.ts + updateGrant.ts — RoleModel.findById(roleId) existe && status 'active' → si no, 400. (Roles son GLOBALES, no tienen accountId → validación = existencia+activo.) Sin infra nueva (los Lambdas usan Role compartido que ya lee Role/Permission/Resource; ROLE_TABLE ya en env).
- **F5-S5 permiso share por object_type** (AC-6): createGrant/deleteGrant/updateGrant — reemplazado assertPermission('access.manage') por assertObjectPermission(event, `${objectType}.share`, objectType, objectId). En delete/update el check va tras traer el grant (usa grant.objectType/objectId). Seed: agregados folder.share/agent.share/table.share (grant a admin). El resolver construye el permiso desde el NOMBRE del Resource (getPermissionsForRole → resource.name), por eso el Resource se llama 'folder.share'. listGrants se dejó en access.view.
- **F5-S6 audit log** (AC-7): tabla dedicada. Nuevo modelo Access/domain/models/AccessAuditLog.model.ts + servicio Access/application/services/AccessAuditLogService.ts (fail-open). Wired en los 3 handlers tras mutación (update incluye changes before/after roleId). Infra: tabla AccessAuditLogTable (PK id, GSI accountId-createdAt-index, SSE reusa AccessGrantTableKmsKey) + env ACCESS_AUDIT_LOG_TABLE. Sin IAM nuevo: los Lambdas de grants usan el Role compartido con dynamodb:* sobre *.

### Prioridad media
- **F5-S7 cascada al borrar user/team**: nuevo AccessGrantModel.deleteAllBySubject(subjectType, subjectId). Wired en Teams/application/delete/deleteTeam.ts (reemplazó TODO(Feature-5)) y User/application/delete/deleteOrgUser.ts. IAM: AccessGrant (Query+DeleteItem) en TeamsDeleteTeamRole y UserDeleteOrgUserRole (env ACCESS_GRANT_TABLE ya estaba).
- **F5-S8 cascada al borrar objeto**: AccessGrantModel.deleteByObjectId wired (fail-open) en deleteFolder.ts, deleteTable.ts, Assistant/application/Delete/index.ts. IAM AccessGrant (Query+DeleteItem) en FolderWriteRole, DtTableWriteRole, RoleChatbotWrite (env ya presente). Nota: en delete recursivo de folder, descendientes se limpian cuando se borren individualmente.
- **F5-S9 invalidar cache de miembros de team** (AC-7): nuevo helper Access/application/_helpers/invalidateGrantSubject.ts (user→ese user; team→todos los miembros vía TeamUserModel.findByTeamId; fail-open). Reemplaza la invalidación que solo cubría user en los 3 handlers. Sin infra (Role compartido + TEAM_USERS_TABLE ya en Globals).

### Herencia (eran AC-4 y AC-5; diferidos y luego hechos)
- **F5-S3 herencia en listGrants** (AC-5): listGrants.ts reescrito — al query por objeto devuelve directos + heredados de carpetas ancestro, cada uno con is_inherited + inherited_from. Resuelve ancestros vía path del folder (para agent/table usa su folderId + ancestros). Un directo gana sobre heredado para el mismo subject. Sin infra (Role compartido + FOLDERS_TABLE/CHATBOT_TABLE/DYNAMIC_TABLES_TABLES_TABLE ya en Globals de Access).
- **F5-S4 bloquear revocar heredado** (AC-4): deleteGrant.ts — acepta ?object_id=<contexto>. Si grant.objectId != contexto → 403 code 'inherited_grant_cannot_be_revoked_here' con inherited_from.

### Baja / producto
- **F5-S11 soft delete** (IMPLEMENTADO): modelo AccessGrant + campos deletedAt+ttl, método softDelete() (marca deletedAt + ttl 90 días), TODAS las lecturas excluyen soft-deleted (FilterExpression attribute_not_exists(deletedAt); findById devuelve null si borrado). Cascadas (deleteByObjectId, deleteAllBySubject) usan softDelete. deleteGrant handler → grant.softDelete() en vez de delete(). access.types.ts: deletedAt+ttl. REEMPLAZA el hard delete. Nota: el softDelete de Folder tiene un bug (divide segundos/1000 otra vez) que NO copié.
- **F5-S12 admin_objects** (COMPLETADO — era STUB): Teams/application/get/getAdminObjects.ts estaba stub (return vacío, TODO Feature-5). Reemplazado: RoleModel.findByName('admin') → AccessGrantModel.findBySubjectId(teamId) → filtrar roleId===adminRole.id + subjectType team + cuenta → agrupar por tipo. Devuelve IDs agrupados { folders: string[], agents: string[], tables: string[] } (decisión de diseño; si el front de 4.1/4.2 necesita objetos completos, extender). Infra Teams: env ROLE_TABLE + IAM AccessGrant(Query)+role(Scan) en TeamsGetUsersRole. Ruta ya existía: GET /teams/{id}/admin_objects.

## DIFERIDO (decidido, tickets aparte)
- **F5-S10 dashboard como object_type**: NO existe entidad dashboard en el backend (sin modelo Dashboard.model.ts, sin tabla -Dashboard; solo ramas de front/RTA). Agregar el enum sin la entidad crea camino muerto (createGrant siempre 404). Diferir hasta que exista el objeto. Se identifica cuando haya un Dashboard.model.ts + tabla ${StackName}-Dashboard.
- **PERMISSION_STRICT_MODE global**: rollout coordinado aparte (ver D2).
- **Cleanup strict-tsc**: el repo NO compila con tsc estricto (build real usa esbuild/SAM laxo). Hay ~30 errores 'chatbotId' sobre object (mismo Utils.objected), 5 colisiones de scope en tests de DynamicTables (mockFindColumnsByTableId), y otros. Ticket chore aparte (o arreglar Utils.objected para tipar bien → limpia los ~30).

## Pre-existentes encontrados (ajenos a Feature 5)
1. Assistant/application/Delete/index.ts:53 — pathParameters.chatbotId sobre object (TS2339). Es patrón de ~30 lugares. REVERTÍ mi parche (inconsistente arreglar 1 de 30).
2. Assistant template — env TEAM_USERS_TABLE duplicado (líneas 147/196) → cfn-lint E0000. ARREGLADO (borré la 196; es fix completo y aislado, en template que Feature 5 ya toca).
3. DynamicTables tests — colisión de scope (falta import/export → script global). REVERTÍ mi export{} (movía el error a otros 4 tests). Va en el ticket chore.

## Tests
Suite Access + Teams getAdminObjects: 20+ tests, todos verdes en corridas puntuales. Archivos: createGrant.test.ts, updateGrant.test.ts, deleteGrant.test.ts, listGrants.test.ts, AccessAuditLogService.test.ts, invalidateGrantSubject.test.ts, AccessGrant.model.test.ts, Teams/.../getAdminObjects.test.ts.

## Archivos cambiados (Feature 5, sin commitear)
Código: Access/{createGrant,updateGrant,deleteGrant,listGrants}.ts, Access/domain/models/AccessGrant.model.ts, Access/domain/models/AccessAuditLog.model.ts (nuevo), Access/application/services/AccessAuditLogService.ts (nuevo), Access/application/_helpers/invalidateGrantSubject.ts (nuevo), Access/domain/types/access.types.ts, Teams/application/delete/deleteTeam.ts, Teams/application/get/getAdminObjects.ts, User/application/delete/deleteOrgUser.ts, Folders/application/Folders/delete/deleteFolder.ts, DynamicTables/application/Tables/delete/deleteTable.ts, Assistant/application/Delete/index.ts, scripts/migrations/seed-access-resources.ts.
Templates (infra): Access, Teams, User, Folders, DynamicTables, Assistant.
Tests: 8 nuevos.

## PENDIENTE OPERACIONAL (para shippear)
1. Commit + push + PR a develop (nada commiteado).
2. Correr seed-access-resources.ts en cada ambiente (crea share).
3. Desplegar 6 módulos: Access (tabla audit + soft delete), Teams, User, Folders, DynamicTables, Assistant.
4. NO verificado: suite completa de tests, prueba en dev real (todo unit+mocks), review del PR (cambio grande cross-módulo).
5. Menor: el método delete() del modelo AccessGrant quedó sin uso (lo reemplazó softDelete); opcional quitarlo.

## Cómo continuar
- Correr suite completa de los módulos tocados → confirmar que no rompí nada.
- Commit (¿por subtarea o agrupado?) → push → PR a develop.
- Correr seed + desplegar los 6 módulos + probar en dev.
- Pendiente: 2 peticiones fuera de scope que el usuario mencionó (aún no detalladas).