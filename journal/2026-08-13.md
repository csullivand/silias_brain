---
tags: [journal, silia]
---

# 2026-08-13/14

## SL-1432 Feature 7 — fix "columna de detalle quitada reaparece"
Rama `feat/SL-1432-user-level-detail-config`. Sesión: [[Claude Sessions/silia/SL-1432-detail-view-reconcile/2026-08-13]]

### Cambios principales
- **Bug:** quitar una propiedad en Configure View no persistía; el `reconcile` de lectura reponía toda columna visible ausente del layout. Reordenar sí guardaba.
- **Fix (raíz):** el layout tiene su propio `updatedAt` (`DetailViewConfig.updatedAt`), marcado sólo al guardarlo; `reconcile` sólo repone columnas creadas después de ese sello (nuevas, Feature 4), deja fuera las que el usuario sacó (Feature 3).
- Resolví un BLOCK real del adversarial-verify: overlay sólo-de-visibilidad reconciliaba el global con el timestamp personal.
- 3 commits pusheados (`379d3b13b`, `453dac312`, `44a3b538e`), 60 tests en 6 suites, tsc limpio.

### Decisiones
- Fix de raíz (layout con su propio timestamp) en vez de parchear síntomas; cerró las 2 objeciones de reviewers.
- Mantuve el borde `<=` para proteger la garantía primaria "lo quitado se queda quitado".

### Descubrimiento aparte (NO arreglado)
- `listGrants` Lambda `Runtime.HandlerNotFound`: la migración webpack→esbuild dejó handlers ANIDADOS (`xMin.x.handler`) en templates; esbuild exporta PLANO (`xMin.handler`). Aplanar 10 handlers (Access + DynamicTables). Concepto: [[concepts/esbuild handler shape - webpack to esbuild migration]]

### Pendiente
1. `sam deploy` de DynamicTables a dev + repetir remove-then-reload para verificar en vivo.
2. Arreglar los handlers webpack→esbuild.
3. Opcional: backfill de `detailViewConfig.updatedAt` en overlays viejos (se auto-cura solo al próximo guardado).