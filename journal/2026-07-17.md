---
tags: [journal, silia]
---

# 2026-07-17

## Feature 0 / Fase 3 — cablear permisos atómicos a endpoints
Sesión: [[Claude Sessions/silia/feature-0-permissions-wiring/2026-07-17]]

### Cambios principales
- **172 handlers** cableados con `assertPermission`/`assertObjectPermission` (o `CASL_PERMISSION` env-var en Billing) en **14 módulos**. Modo permisivo, piso `assertRole` preservado.
- Billing ya estaba cableado (patrón Middy) — solo alineé 2 valores (`account.invoices.view`, `agent.billing.edit`).
- Cableado masivo delegado a **subagentes en paralelo** (1 módulo/agente, mapeo permiso-por-handler explícito).
- Doc `permission-mapping-for-pms.md` pasó de propuesta tentativa a **estado de cableado real** por módulo.

### Decisiones (PM, 1x1)
- Cableado: ChunkMetadata (guard por método), Metrics /get+/aggregated (agent.metrics.view), PUT /conversation (→agent.inbox.reassign).
- Solo assertRole: audiences, Integrations, ExecuteWorkflow.
- Confirmados: business-rules/tool-schema/wizard (settings.*), Flows actions (.edit), updateAccountStatus (information.edit), invoices.download (cubierto por view).
- **Matriz inbox Operador validada**: opera el inbox salvo reassign.

### Pendiente
- Deploy + seeds (B1→B2→B4) + PR. Fase 6 = flip PERMISSION_STRICT_MODE.

### Aprendizaje
- `git stash` de subagentes + hooks auto-commit RTA revirtieron trabajo sin commitear → commitear por módulo apenas termina cada agente; excluir submódulos.