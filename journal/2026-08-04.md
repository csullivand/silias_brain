# 2026-08-04

## Silia — SL-1526 KPI prefs (Feature 5) + recuperación de archivos

### Cambios principales
- Nueva rama **`feat/SL-1526-persistencia-kpis-usuario-tabla`** = develop + cherry-pick `a969064ab` (KPI prefs Feature 5.1) → commit `bf63ab643`.
- Commit `273d17a11`: **quité `accountId`** del modelo KPI prefs — no se usa y venía del authorizer (cuenta home, mal para superadmin en cuenta cambiada, SL-1278). Alineado con PRD spec `(user_id, data_view_id)`. 19/19 tests.
- Doc FE `docs/kpi-prefs-frontend-integration.md` creado (untracked, sin commitear a pedido).

### Decisiones
- KPI prefs = tipo entidad hija scoped por tabla (como DynamicRow) → NO lleva accountId. Si se necesitara: `access.table.accountId` + GSI, nunca el authorizer.
- Feature 6 (agregaciones) = ticket aparte; reutilizar `KPI_KINDS`/`KPI_RECORDS_ID` de `tables.types.ts`.

### Aprendizajes
- Los 4 access-control docs "perdidos" en realidad están en `.gitignore` (keep local) → no commiteables, no fue pérdida por branch switch.
- `yarn` roto sin `GH_PACKAGES_TOKEN` → `GH_PACKAGES_TOKEN=dummy npx jest`.
- pr-review.sh / adversarial-verify.sh generan prompt+diff para que Claude evalúe; ambos dieron Approved/PASS.

### Pendiente
- Push + PR de SL-1526 (descripción en bullets no-técnica lista).
- Feature 6 endpoint de agregaciones.

Relacionado: [[Claude Sessions/silia/SL-1526-kpi-prefs-persistence/2026-08-04]] · [[Claude Sessions/silia/SL-1557-rbac-strict/2026-08-04]]