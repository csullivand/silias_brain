# 2026-08-06

## Silia — Análisis + implementación: Manual escalation (SL-1579 / Feature 1)

### Qué se hizo
- Análisis del sistema de escalaciones del inbox (doc con diagramas): [[projects/Escalacion Forzada Admin - Analisis y Plan]].
- **Implementación backend** en rama `feat/SL-1579-force-escalation`: endpoint `POST /{chatbotId}/escalations/force` — escalación manual que toma control.
- Comportamiento (tras iterar con el ticket): auto-asigna al operador (`in_progress`), idempotente (toma control del ticket existente, sin 409), permiso NUEVO `agent.inbox.force_escalate` para todos los roles del inbox, +2 columnas auditoría `isForced`/`forcedBy`.
- Permiso nuevo sembrado en el catálogo backend (JSON) + matriz rol→permiso + catálogo FE; tests de catálogo 102→103.
- Tests: 23/23 verdes. Handler empaqueta (build entry agregada).
- Guía FE para Camila: `docs/escalacion-forzada-frontend-integration.md`.

### Decisiones (evolución)
- solo admin → permiso nuevo para todos; sin asignar → auto-asigna; 409 → idempotente take-control.

### Pendiente
- Sembrar permiso en BD antes de habilitar; commitear; implementación FE.

Relacionado: [[Claude Sessions/silia/escalacion-forzada-admin/2026-08-06]]