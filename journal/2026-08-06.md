# 2026-08-06

## Silia — Análisis escalaciones inbox + plan escalación forzada por admin

### Qué se hizo
- Análisis exhaustivo del sistema de escalaciones (cómo funciona hoy) + plan de la "escalación forzada por admin". Doc con diagramas mermaid: [[projects/Escalacion Forzada Admin - Analisis y Plan]] (repo `docs/escalacion-forzada-admin-analisis-y-plan.md`).

### Hallazgos clave
- escalación == ticket (`EscalationRecord`). Hoy solo LLM escala; no hay creación manual.
- El gate de horario vive SOLO en el evaluador LLM; `createEscalation` no valida horario → la feature = componer `createEscalation` + `assignToAgent` detrás de un endpoint admin.

### Decisiones
- Permiso `agent.inbox.force_escalate` (admin+); bloquea conversación; 409 si ya hay escalación activa; identidad del token; +2 columnas `isForced` y `forcedBy`/`forcedByName`.

### Pendiente
- Confirmar origen de `userName`; nº de ticket; implementar.

Relacionado: [[Claude Sessions/silia/escalacion-forzada-admin/2026-08-06]]