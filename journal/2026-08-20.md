---
tags: [journal, silia, roles, feature-11, SL-1293]
date: 2026-08-20
---

# 2026-08-20 — [Feature 11] SL-1293 Role dropdowns: casing + superadmin + ShareAccess

Sesión larga cerrando bugs de asignación de rol. Nota: [[Claude Sessions/silia/SL-1293-role-dropdowns-casing/2026-08-20]]

## Cambios (2 commits pusheados a feat/SL-1293-include-custom-roles-dropdown)
- **Casing case-insensitive de rol** en todo el ciclo: resolver, asignar, contar, listar, filtrar, reasignar. user.role se guarda en minúsculas. (Arreglaba el bug 'sulli' → role_not_found.)
- **Superadmin puede asignar cualquier rol** también a usuarios (POST/PATCH /management), no solo en grants. (Bug de Camila: 'role superadmin cannot be assigned'.)
- **POST /role/{id}/reassign** — reasignación masiva de usuarios+grants entre roles.
- **ShareAccessModal (FE):** el grant ahora toma el rol del usuario seleccionado, no operator hardcodeado. Diagnóstico validado CONTRA LA BD DEV (el POST del FE mandaba operator).
- **Fixes de review:** invalidación de cache case-insensitive (clearAbilityCache) + filtro de rol en Org>Users in-memory (usuarios mixed-case legacy no desaparecen — BLOCK del adversarial).

## Decisiones
- Superadmin asigna todo (tensión con AC, decisión del usuario).
- ShareAccessModal deriva del usuario, sin picker.
- Commits sin docs/Agent/Voice (submódulos).

## Validación con BD dev
- Perfil AWS 'silia' (SSO). Stack dev-app-silia-com. Confirmado: el BE guarda/devuelve fielmente el roleId que manda el FE; el 'siempre operator' era del FE.

## Pendiente
- PR + sam deploy a dev.
- Teams en ShareAccessModal (hoy operator).
- 2 LOW cosméticos sin aplicar.