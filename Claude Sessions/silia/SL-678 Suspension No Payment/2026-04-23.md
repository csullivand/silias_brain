---
tags: [claude-session, silia, billing, retrocompatibility]
---

# Session: 2026-04-23 — SL-678 Billing Retrocompatibility for Existing Accounts

## Context
- **Branch:** feat/SL-678-suspension-no-payment
- **Working directory:** /Users/sulli/Projects/silia/06-11-25/silia
- **Ticket/Issue:** SL-678
- **Goal for this session:** Ensure billing system works retroactively with existing accounts that were created before billing was implemented.

## Work Log (step by step)

### Step 1: Retrocompatibility Analysis
- **What:** Deep exploration of the entire billing module to identify what breaks for pre-billing accounts
- **Why:** Existing accounts have no stripeCustomerId, no billingPlan, no stripeSubscriptionId — needed to map all failure points
- **Files explored:** Billing/application/handlers/StripeWebhookHandler.ts, Billing/infrastructure/aws/handlers/createSubscription/, Billing/infrastructure/aws/handlers/onboardAccount/, Accounts/domain/models/Account.model.ts, Accounts/application/usageFees/getUsageFees.ts, Assistant/domain/models/Chatbot.model.ts
- **Result:** Identified 5 breaking points: missing stripeCustomerId (HIGH), missing billingPlan on Account (HIGH), missing billingPlan on Chatbot (MEDIUM), missing stripeSubscriptionId (MEDIUM), no GSI on stripeCustomerId (LOW)

### Step 2: Design Decision — Unified Flow at Chatbot Level
- **What:** Decided to handle retrocompatibility at the chatbot PUT handler level instead of creating migration endpoints
- **Why:** User's insight — when updating chatbot billing, if no subscription exists, create one. Same endpoint handles both new and existing accounts. No migration scripts needed.
- **Result:** Plan approved — single file change in Assistant/application/Put/index.ts

### Step 3: Implementation — Auto-subscribe on billing plan update
- **What:** Added else-if branch in Assistant/application/Put/index.ts (line ~468) that auto-onboards accounts and creates subscriptions when chatbot has no stripeSubscriptionId
- **Why:** Pre-billing accounts get automatically onboarded + subscribed when someone updates their chatbot's billing plan
- **Files touched:** Assistant/application/Put/index.ts (lines 38, 468-518)
- **Changes:**
  1. Added import: AccountModel from Accounts/domain/models/Account.model
  2. Added else-if branch after existing Stripe sync block:
     - Fetches account via AccountModel.findById(existingChatbot.accountId)
     - If no stripeCustomerId → calls billingService.createCustomer() to onboard
     - Creates subscription via billingService.createSubscription()
     - Saves stripeSubscriptionId and taxRate on chatbot via updateData
  3. Non-blocking (try/catch), same pattern as existing sync code
- **Result:** +51 lines added, compiles cleanly

## Files Modified This Session
| File | What changed | Why |
|------|-------------|-----|
| Assistant/application/Put/index.ts | Added AccountModel import + else-if auto-subscribe branch (~50 lines) | Retrocompatibility: auto-onboard and subscribe pre-billing accounts when chatbot billing plan is updated |

## Decisions Made
- Handle retrocompatibility at chatbot PUT handler level, not with migration endpoints
- Non-blocking: if Stripe fails, billing plan still saves locally
- Uses payment_behavior: 'allow_incomplete' so subscription works even without payment method
- Tax rate defaults: MX=16%, else 0% (same as existing pattern)
- stripeSubscriptionId saved via updateData in same DB write

## Problems / Errors Encountered
- Initial edit had a stray closing brace causing else-if to be disconnected — fixed immediately

## Current State (last save)
- **Branch status:** Uncommitted changes in Assistant/application/Put/index.ts (+51 lines)
- **What's working:** Auto-subscribe logic implemented, follows existing patterns
- **What's broken/incomplete:** Not yet tested, not committed

## How to Pick Up From Here
1. Test the 4 scenarios: new account (no Stripe), onboarded account (has customer, no sub), subscribed chatbot (update plan), pending future date
2. Commit changes on feat/SL-678-suspension-no-payment branch
3. Consider adding guard for pending future dates in the else-if branch (currently only the existing if branch checks isPendingFuture)
4. Consider if pause/resume block (line 520) needs updating for newly created subscriptions


### Step 4: Added pendingEffectiveFrom guard
- **What:** Added isPendingFuture check to the auto-subscribe else-if branch
- **Why:** Without it, setting a future effectiveFrom would create a subscription and start charging immediately instead of waiting for the scheduled date
- **Files touched:** Assistant/application/Put/index.ts (lines 470-475)
- **Changes:** Added if/else around the try block — if isPendingFuture, log and skip; else try to auto-subscribe
- **Result:** +56 lines total (was +51), consistent with existing branch's guard pattern


### Step 5: Fix Stripe tax location error
- **What:** Changed country metadata fallback from '' to account.country || mergedPlan.country || 'MX'
- **Why:** Stripe rejected subscription creation with customer_tax_location_invalid because account had no country set. StripeGateway.createSubscription reads country from metadata to update customer address before enabling automatic_tax.
- **Files touched:** Assistant/application/Put/index.ts (line 508)
- **Result:** Country now falls back to MX (Mexican platform default)

### Pending: Update account billingPlan
- User pointed out we're not updating the Account's billingPlan or country during auto-onboard — only saving stripeCustomerId. Need to also persist billingPlan and country on the account record.


### Step 6: Disable Change Plan button when account has no billing plan
- **What:** Added conditional rendering for the Change Plan button in BillingPlan component
- **Why:** If the account has no billingPlan (pre-billing account), clicking Change Plan would fail. Better to disable it and show a tooltip explaining why.
- **Files touched:** app/src/features/account/components/AccountManagement/AccountManagement.tsx (~line 1255)
- **Changes:** Wrapped Change Plan button in conditional: if !account?.billingPlan?.monthlyBaseFee → disabled button with Tooltip 'Need to update account billing plan'; else → normal clickable button. Uses same span wrapper pattern as DataViewsPlaceholder.tsx for tooltip on disabled button.
- **Result:** +52/-11 lines in AccountManagement.tsx


### Step 7: Fix frontend build errors
- **What:** Modified app/config-overrides.js and package.json to fix compilation errors
- **Why:** yarn start-DEV failed with 13 errors: missing @shared/inbox, @shared/ui, @shared/widget packages and @opentelemetry version mismatches
- **Files touched:** app/config-overrides.js (+30/-), package.json (+4)
- **Result:** Build fix in progress


### Step 8: Changes reverted
- **What:** Assistant/application/Put/index.ts and AccountManagement.tsx changes were reverted (no longer in git diff)
- **Why:** Unknown — user may have reset or checked out those files
- **Current state:** Only yarn.lock, config-overrides.js, and package.json changes remain


### Step 9: Fix disabled button check (2026-04-27)
- **What:** Changed Change Plan disabled condition from !account?.billingPlan?.monthlyBaseFee to !account?.stripeCustomerId
- **Why:** Account model constructor always sets default billingPlan with monthlyBaseFee: 99.99, so the previous check was never falsy. stripeCustomerId is only set when account is actually onboarded to billing.
- **Files touched:** app/src/features/account/components/AccountManagement/AccountManagement.tsx (line 1256)
- **Result:** Change Plan button now correctly disabled for non-onboarded accounts


### Step 10: Auto-onboard Stripe customer on account update (2026-04-27)
- **What:** Added auto-onboard logic to Accounts/application/put/index.ts
- **Why:** When updating account with billingPlan, stripeCustomerId was never set. Change Plan button checks stripeCustomerId so it would stay disabled forever. Now when billingPlan is set and no stripeCustomerId exists, we auto-create the Stripe customer.
- **Files touched:** Accounts/application/put/index.ts (+20 lines)
- **Changes:** Added BillingService import, after account update check if billingPlan in body && !stripeCustomerId → call billingService.createCustomer() → save stripeCustomerId. Non-blocking try/catch.
- **Result:** Full flow now: update account → Stripe customer created → stripeCustomerId saved → Change Plan enabled → change plan creates subscription


### Step 11: Add payment method success notification (2026-04-27)
- **What:** Added sendPaymentMethodAddedNotification call on successful card addition
- **Why:** The method existed in NotificationService (line 170) but was never wired up in the addPaymentMethod handler. Only failure notification was connected.
- **Files touched:** Billing/infrastructure/aws/handlers/addPaymentMethod/addPaymentMethod.ts (+15 lines)
- **Changes:** After successful addPaymentMethod, send notification with cardBrand and last4 from result. Non-blocking try/catch.
- **Result:** Users now get email when a card is successfully added


### Step 12: Fix setup_intent.succeeded Stripe API error (2026-04-27)
- **What:** Changed subscriptionSchedules.list parameter from status: 'not_started' to scheduled: true
- **Why:** Stripe API version 2024-12-18.acacia doesn't accept 'status' as a filter parameter on subscriptionSchedules.list. The correct parameter is 'scheduled: true'. Error was: 'Received unknown parameter: status'
- **Files touched:** Billing/application/handlers/StripeWebhookHandler.ts (line 665)
- **Result:** setup_intent.succeeded webhook handler will now work correctly, setting default payment method and updating pending schedules


### Step 13: Add perMinuteRate and agentType fields (2026-04-28)
- **What:** Added perMinuteRate to BillingPlan type/model and agentType to ChatbotModel
- **Why:** Support for RTA (real-time voice agent) billing — voice agents need per-minute rate tracking and chatbots need to distinguish between conversational and RTA agent types
- **Files touched:**
  - Accounts/domain/types/types.ts: Added perMinuteRate optional field to BillingPlan interface
  - Accounts/domain/models/Account.model.ts: Added perMinuteRate to BillingPlanValidator with @IsNumber @IsOptional decorators
  - Assistant/domain/models/Chatbot.model.ts: Added perMinuteRate to billingPlan in ChatbotI interface, added agentType field ('rta' | 'conversational'), added to model class with decorators, constructor default 'conversational', and toJSON output
- **Result:** Model layer ready for RTA billing support


### Step 14: RTA (voice agent) billing support — full stack (2026-04-28)
- **What:** Added perMinuteRate and agentType across backend models, schemas, transformers, and frontend
- **Why:** Voice/RTA agents use per-minute billing instead of conversation-based billing. Need to distinguish agent types and calculate costs differently.
- **Files touched:**
  - **Backend models:** Accounts/domain/types/types.ts (perMinuteRate on BillingPlan), Accounts/domain/models/Account.model.ts (BillingPlanValidator), Assistant/domain/models/Chatbot.model.ts (agentType + perMinuteRate in billingPlan)
  - **Backend schemas:** Assistant/application/schemas/chatbot.schema.ts (ChatbotResponseSchema + ChatbotListItemSchema: perMinuteRate, agentType, minutesUsed, usageCost)
  - **Backend transformers:** Assistant/application/Get/transformers.ts (RTA billing calc: minutes-based with no base fee vs conversational: base fee + overage)
  - **Backend PUT:** Assistant/application/Put/index.ts (UpdateChatbotSchema: perMinuteRate + agentType)
  - **Frontend types:** app/src/features/account/types.ts (perMinuteRate on AccountData + NewAccountFormData)
  - **Frontend utils:** app/src/features/account/utils/assistantBillingUtils.ts (ChatbotBillingPlanApi + ChatbotListItemForBilling: RTA fields)
  - **Frontend UI:** app/src/features/account/components/AccountManagement/AccountManagement.tsx (+596/-197 — major RTA billing UI changes)
- **Result:** 12 files changed, +542/-197 lines. Full stack RTA billing support.


### Step 14b: RTA billing frontend utilities + UI expansion (2026-04-28)
- **What:** Added computeRtaBillingFromChatbot() and formatUnitCost() to assistantBillingUtils.ts; expanded AccountManagement.tsx UI significantly (+718 lines vs original)
- **Why:** Frontend needs dedicated helpers for RTA per-minute billing display (minutes used, per-minute rate, usage cost) separate from conversational billing. UI expanded to show RTA-specific billing cards/sections.
- **Files touched:**
  - app/src/features/account/utils/assistantBillingUtils.ts: +computeRtaBillingFromChatbot (extracts minutesUsed, perMinuteRate, usageCost, taxes, total), +formatUnitCost (3 decimal places for per-minute rates)
  - app/src/features/account/components/AccountManagement/AccountManagement.tsx: Major UI expansion for RTA billing display
- **Result:** +686/-197 lines total across 12 files


### Step 15: Fix country not resolved when updating billing plan (2026-04-29)
- **What:** Added Account lookup fallback for country in the existing-subscription billing sync path
- **Why:** When updating a chatbot's billing plan (existing subscription), the code tried to get country from mergedPlan.country or existingPlan.country — but chatbot billingPlan doesn't store country. It lives on the Account model. So taxRate was never being set correctly for existing subscriptions.
- **Files touched:** Assistant/application/Put/index.ts (line ~464-472)
- **Changes:** Changed const→let for country, added if(!country) block that fetches AccountModel.findById(existingChatbot.accountId) to get account.country as fallback
- **Also identified:** createSubscription metadata passes account.country || mergedPlan.country || '' — if both are empty, Stripe customer gets no address and automatic_tax fails. Discussed adding 'MX' default fallback.
- **Result:** Tax rate now correctly resolved from account country for existing subscriptions


### Step 15b: Reverted country fallback (2026-04-29)
- **What:** Reverted the Account lookup fallback for country in existing-subscription billing sync
- **Why:** User requested removal. Country resolution approach needs rethinking.
- **Files touched:** Assistant/application/Put/index.ts (line ~464)
- **Result:** Back to original: const country = mergedPlan.country || existingPlan.country


### Step 16: Fix auto-subscribe to handle future-dated subscriptions (2026-04-29)
- **What:** Replaced the 'skip' logic for future dates in the auto-subscribe path with actual Stripe scheduled subscription creation
- **Why:** When updating a chatbot's billing plan with a future effectiveFrom and no existing subscription, the code just logged 'Skipping auto-subscribe' and created nothing in Stripe. The pendingEffectiveFrom was saved locally but no schedule was created.
- **Root cause:** The auto-subscribe path (line ~470) had isPendingFuture check that skipped everything. The original create-assistant flow uses POST /billing/subscriptions which has schedule logic, but the Change Plan flow goes through PUT /chatbot/{id} which didn't.
- **Files touched:** Assistant/application/Put/index.ts (+78 lines)
- **Changes:**
  1. Added import: StripeGateway from Billing/infrastructure/gateways/StripeGateway
  2. Removed isPendingFuture skip — now always proceeds to create subscription
  3. Reads pendingEffectiveFrom or effectiveFrom, converts to Unix timestamp
  4. If future date: resolves customer's default payment method → calls stripeGateway.createScheduledSubscription()
  5. If immediate: calls billingService.createSubscription() as before
  6. Both paths save subscription/schedule ID and taxRate on chatbot
- **Pattern:** Mirrors POST /billing/subscriptions endpoint logic (lines 109-135 in createSubscription handler)
- **Result:** Change Plan with future date now creates a Stripe schedule instead of doing nothing

### Note: Files reverted
- Several files from Steps 13-14 (RTA billing: transformers, schemas, ChatbotModel, AccountManagement, billingUtils) appear to have been reverted or committed separately by user — no longer in git diff


### Step 16b: Refined scheduled subscription code + reverted (2026-04-29)
- **What:** Aligned PUT handler auto-subscribe scheduled path with existing createSubscription endpoint: country default 'MX', added NotificationService import + email notification, removed inline require(). Then Put/index.ts was reverted/committed separately.
- **Comparison result:** Scheduled path matches existing endpoint for all scenarios. Immediate path has pre-existing gaps (missing defaultPaymentMethodId, zipCode, billingCycleAnchor) that were there before our changes.
- **Current state:** Put/index.ts no longer in git diff — reverted or committed by user