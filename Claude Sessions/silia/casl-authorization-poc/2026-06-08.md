---
tags: [claude-session, silia, authorization, casl, folders, logger]
created: 2026-06-08
updated: 2026-06-16
---

# Session: 2026-06-08 — CASL + Folders + Logger

## CASL Authorization (Story 0 + 0.1)

### Completed
- Permission model expanded (PermissionLevel + PermissionCategory + description/category)
- Role enum (SUPERADMIN, SUPERVISOR, OPERATOR)
- CASL ability builder (can('allowed', 'permission.string'))
- Middleware reads CASL_PERMISSION env var from SAM template
- Permissive/strict feature flag (PERMISSION_STRICT_MODE)
- GET /me/permissions endpoint + SAM entry
- GET /permissions_catalog endpoint + SAM entry
- Protected default roles from modification
- Multi-resource helpers (checkPermission, checkAll, checkAny)
- Async job permission capture (capturePermissionContext)
- Developer guide (docs/permission-developer-guide.md)
- PM mapping document (docs/permission-mapping-for-pms.md)
- Seed script with categories (scripts/seed-permissions.sh)
- Cleanup script (scripts/cleanup-permissions.sh)
- Billing module wired (14 handlers)
- 211 parametric tests
- DB seeded: 62 PRD permissions, 4 roles

### Blocked
- Apply to remaining modules — needs PM mapping (which Lambda = which PRD permission)

## Folders CRUD

### Completed
- Full module: create, list, get, rename, move, delete
- Cross-tenant guards on all handlers
- Permission checks (folder.create/view/edit/move/delete)
- Async bulk ops via SQS for large trees (>50 descendants)
- Soft delete with 30-day TTL
- Max 5 levels depth validation
- Audit logging

### Bugs Fixed (from PR reviews)
- Cross-tenant read in getFolderById + getFolderStatus
- Path mutation in move-to-parent delete (capture oldChildPath before updateFields)
- Async MOVE stale depth (oldDepth in SQS message)
- Invalid pageToken crash (try-catch returns 400)
- move-to-parent child truncation at 50 (pagination loop)
- childrenCount cap at 50 (pagination loop)
- Error message leak in listFolders (guard on statusCode)
- SQS VisibilityTimeout 180→960s
- total→count in list response
- KMS perms for BillingAuditLog CMK table
- SQS encryption with CMK (CKV_AWS_27)
- DynamoDB CMK + PITR (CKV_AWS_119, CKV_AWS_28)
- Postman test total→count

### Frontend Integration
- Guide created: docs/folders-frontend-integration-guide.md
- FE currently mock-driven, needs to wire folderService to real API

## Logger Fix

### Problem
- logger.info() calls (195 across codebase) suppressed in CloudWatch
- Default Pino level was 'warn' for non-dev environments
- console.log() worked because it bypasses Pino
- pino-logger.ts is unused (0 imports) — red herring

### Fix
- shared/observability/logger.ts line 73: 'warn' → 'info'
- One line change, no SAM template changes needed
- LOG_LEVEL env var still works as per-module override
- Verified locally: info/warn/error all output to stdout

## Prompts Added
- PR Review Prompt → CLAUDE.local.md + Obsidian
- Adversarial Verify Prompt → CLAUDE.local.md + Obsidian
- Auto-Improvement Prompt → CLAUDE.local.md + Obsidian