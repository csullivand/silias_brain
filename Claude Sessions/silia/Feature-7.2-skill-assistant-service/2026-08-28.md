---
tags: [claude-session, silia, skills, feature-7, backend]
---

# Session: 2026-08-28 — [Feature 7.2 BE] Skill Assistant service (contexto del agente + conversación persistente)

## Context
- **Branch:** develop (starting point — new ticket, no feature branch yet)
- **Working directory:** /Users/sulli/Projects/silia/06-11-25/silia
- **Ticket/Issue:** Feature 7.2 [BE] — Servicio del Skill/Flow Assistant. Domain: Platforma (SL). PRD: PRD - Skills. Depends on Feature 3.3 (contrato de instrucciones) + servicio de IA.
- **Goal for this session:** Review the PRD + review the existing Skills submodule code to establish what exists vs what must be built for the Skill Assistant backend service. NO implementation yet — scoping/analysis pass.

## The ticket (Feature 7.2 BE)
Endpoint POST /api/agents/:id/flows/:flowId/assistant with {message, mode(flow|rules), flow_context}; response {reply, action?: {type, payload}}.
- action.type ∈ insert_instructions | edit_instructions | add_rule | edit_rule | create_integration | edit_integration | create_mcp | edit_mcp
- Conversation persists per skill across sessions/days; reset ONLY via DELETE .../assistant/conversation.
- Assistant operates with FULL agent context (setup, personality, global rules, channels, resources); write capability scoped to skills (instructions, business rules, custom integrations, MCP servers) — always proposals, FE applies.
- System prompt with per-tab behavior (instructions vs rules).

## Key insight
The custom-integrations CODE assistant (Feature 4.2) is ALREADY built in Skills/application/CustomIntegrations/Assistant/ (get/post/reset lambdas + CustomIntegrationConversations Dynamo table). This is the TEMPLATE to mirror for the Skill Assistant — same conversation-persistence pattern, different prompt/role + structured actions instead of code artifacts.

## Work Log (step by step)

### Step 1: Map Skills submodule
- **What:** Explored Skills/ submodule structure.
- **Files touched:** (read-only) Skills/application, Skills/domain
- **Result:** application/ has CustomIntegrations/{Assistant,Delete,Execute,List,Save}, Flows/{Create,Delete,Get,List,Update}, McpServers/{Connect,Disconnect,List,Test}. domain/ has customIntegrations, mcpServers, models, processFlows, processInstructions, skills. NO Flows/Assistant or Skill Assistant exists yet → Feature 7.2 is greenfield, mirroring CustomIntegrations/Assistant.
- Skills is a git submodule (own .git, own history). Latest commit bf4f500.

### Step 2: Deep-read reference impl + flow context (in progress)
- Launched 2 Explore agents: (1) custom-integrations Assistant impl + infra (conversation persistence, LLM call, DTOs, SAM template); (2) Flows/Skills CRUD + domain model + how agent context is reachable.

## Decisions Made
- (pending review completion)

## Current State (last save)
- **Branch status:** develop, no changes yet. Pre-existing unrelated working-tree changes (Agent/Voice submodule pointers, untracked docs/).
- **What's working:** N/A — analysis only.
- **What's broken/incomplete:** review in progress.

## How to Pick Up From Here
1. Read agent findings on CustomIntegrations/Assistant pattern (persistence table, LLM client, middleware).
2. Read agent findings on Flow/Skill model shape + how to reach parent agent context.
3. Produce the gap analysis: what exists vs what Feature 7.2 needs (lambdas, Dynamo table, structured-action generation, agent-context injection).



### Step 3: Reference impl mapped (CustomIntegrations/Assistant) — TEMPLATE
- **Handlers** (Middy-less plain APIGatewayProxyEvent handlers): Get/index.ts, Post/index.ts, Reset/index.ts. Auth via getCallerContext(event)→userId, LOCAL_DEV_USER_ID bypass when isLocalAuthBypass().
- **Persistence:** services/conversationStore.ts + domain/models/CustomIntegrationConversation.model.ts (extends DB). Table ${StackName}-CustomIntegrationConversations, PK=id, GSI userIntegrationKey-createdAt-index (HASH userIntegrationKey=`${userId}#${integrationId}`, RANGE createdAt). Status active|closed, NO TTL, PITR on, SSE on, PAY_PER_REQUEST. One active conv per (userId,integrationId). Atomic append w/ ConditionExpression status=active AND turnCount<MAX(100). Turns embedded array.
- **Flow:** resolveConversationForTurn → appendTurn(user) BEFORE llm → LLM → appendTurn(assistant) AFTER. User turn saved first so LLM failure doesn't lose input.
- **LLM:** services/litellmCompletionAdapter.ts — LiteLLM Responses API POST {baseUrl}/openai/v1/responses, stream SSE, model default openai/gpt-5.4-mini, temp 0.3, max_output_tokens 2048. Env: CUSTOM_INTEGRATION_LLM_BASE_URL / _MODEL / _API_KEY / _API_KEY_SSM_PARAM / _TIMEOUT_MS.
- **Structured output via function-calling** (NOT regex): codeArtifactTool.ts defines tool 'propose_integration_code'; parseFunctionCall.ts extracts first function_call from output[], JSON.parse arguments; fallback extractOutputText for plain reply. THIS is the mechanism to reuse for the {reply, action} contract.
- **Errors:** infrastructure/utils/errors.ts — ApiError subclasses: ValidationError(400), NotFoundError(404), UnauthorizedError(401), ConflictError(409), AssistantGenerationError(502). response.ts successResponse/errorResponse/originFromEvent.
- **Infra:** aws.template.yml — 3 lambdas 29s/512MB nodejs22.x in VPC, cross-stack authorizer imported ${StackName}-auth-middleware-arn, CORS AllowOrigin '*'. RoleSkills grants DynamoDB on the conversations table + SSM GetParameter. Routes GET/POST /custom-integrations/{integrationId}/assistant, POST .../reset.
- NOTE: ticket endpoint shape is POST /api/agents/:id/flows/:flowId/assistant (agent+flow scoped) — DIFFERENT from custom-integrations (/skills/custom-integrations/{id}). And reset is DELETE .../assistant/conversation per PRD, vs POST .../reset here. Need to decide alignment.



### Step 4: Flow/Skill model + agent-context reachability mapped
- **ProcessFlow** (domain/processFlows/types.ts): {id, agentId, name, description, intent, instructions:InstructionDocument, status:'draft'|'active', schemaVersion:1, rules:ProcessFlowRule[], createdAt/updatedAt ISO}. Table ${StackName}-Process-Flows, PK id, GSI agentId-updatedAt-index. Repo DynamoProcessFlowRepository (listByAgent/getById/create/update/remove).
- **InstructionDocument** (domain/processInstructions/types.ts): {schema_version, segments[]} where segment ∈ text | integration_chip | mcp_chip | table_op_chip (NOTE: agent_chip/step_ref/rule_ref from PRD NOT yet modeled — only 3 chip types + text). Forbidden secret keys enforced on save.
- **ProcessFlowRule**: {id, name, description, content}.
- **McpServer** (domain/mcpServers): {id, agentId, accountId, name, serverUrl, authMethod, credentials(KMS-sealed), tools:McpToolDeclaration[]...}. Table Mcp-Servers, GSI agentId-index. listByAgent.
- **AppliedCustomIntegration** (domain/customIntegrations): {id, accountId, name, description, code, params:IntegrationParam[], timeoutMs, inCatalog...}. Table Custom-Integrations, GSI accountId-index. listByAccount (catalog only).
- **CRITICAL — agent context:** Skills module does NOT read agent data. domain/skills/types.ts SkillAgentContext is only *accepted* from caller (existingSkillIds, availableTools, availableIntents) for validation. Agent personality / global business rules / channels / setup live in the Agent module (separate submodule, no-fly for bot) and are NOT reachable from Skills today. → Feature 7.2 must either (a) have FE pass full agent context in flow_context body (matches PRD endpoint {message,mode,flow_context} + custom-integrations 'context' pattern), or (b) add a cross-module read of Agent config. DECISION NEEDED.

### Step 5: GAP ANALYSIS (what 7.2 needs) — see Active Context / response to user



### Step 7: Dev respondió las 6 preguntas → diseño cerrado + plan escrito (2026-08-31)
- Todas las decisiones confirmadas (ver Active Context). Back lee Chatbot; servicio no escribe skill; action=parche; alcance 4 acciones; tope 100 sin TTL; reuse LiteLLM.
- **Entregable creado:** docs/skill-assistant-7.2-plan.md — plan de implementación completo (13 secciones: objetivo, decisiones, contrato API, modelo de datos, agentContext read Chatbot, system prompt per-tab, function-calling tools, archivos a crear/tocar, flujo POST, testing, fuera de alcance, checklist, preguntas menores).
- Único trozo sin precedente en Skills: leer la tabla Chatbot (agentContext.ts) — se replica estilo Voice (submódulo no puede importar Assistant/domain/models/Chatbot.model.ts por path).
- AWAITING aprobación del plan antes de tocar código.


### Step 8: Adversarial review del plan vs código real → 6 correcciones aplicadas (2026-09-01)
Verifiqué las claims del plan contra el código. Hallazgos:
- **WRONG (corregido):** afirmé que Skills (submódulo) NO puede importar de Assistant/ por path relativo. FALSO — Voice (también submódulo) importa 'from ../../../Assistant/domain/models/VoiceAgentConfig.model' (tipo Y clase). → agentContext.ts puede importar el tipo Chatbot, no replicar a ciegas.
- **WRONG (corregido):** convención de nombre de tabla. Voice usa env CHATBOT_TABLE (process.env.CHATBOT_TABLE || 'dev-app-silia-com-Chatbot'), no stackResourceNameOrDefault. Tabla real: ${StackName}-Chatbot. Precedente IAM listo: policy asst-chatbot-read-policy del rol ${StackName}-asst-chatbot-read-role.
- **BLOQUEANTE (flagged):** ¿agentId (path, ProcessFlow.agentId) == chatbotId? Skills NO tiene chatbotId en ningún lado; todo por agentId. Si son el mismo id (probable: agente=chatbot), chatbotId sobra en el body → derivar del path. Si no, falta el mapeo. Define el request. → banner bloqueante al inicio del plan + §13 #0.
- **Refinamiento:** env LLM renombrado CUSTOM_INTEGRATION_LLM_* → SKILLS_LLM_* (mismo proxy, nombre neutro).
- **Refinamiento:** response GET/Reset ahora espeja verbatim el shape shippeado del code assistant (con su inconsistencia poblada={id}/vacía={conversationId:null}), integrationId→flowId, artifact→action.
- **Refinamiento:** lectura de recursos MCP/integrations = OPCIONAL (los InstructionSegment ya traen display_label) → menos IAM.
- Todas aplicadas a docs/skill-assistant-7.2-plan.md. Arquitectura sin cambios; el reference-clone sigue siendo el approach. Único abierto: agentId==chatbotId.



### Step 9: 2da review — BLOQUEANTE RESUELTO + riesgo de coupling (2026-09-01)
- **agentId == chatbotId == Chatbot.id (PK HASH) — CONFIRMADO por código.** Evidencia: app/src/features/agent-skills/router.tsx dice que el módulo se monta bajo 'accounts/:accountId/assistants/:AssistantID'; el route de channels usa el mismo :AssistantID; ChatbotTable (Assistant/infrastructure/aws.template.yml:3676) KeySchema PK=id. → chatbotId FUERA del body; contexto = GetItem Chatbot id=agentId (path). El row trae accountId → lecturas opcionales de recursos encadenan gratis.
- **Riesgo cazado:** renombrar CUSTOM_INTEGRATION_LLM_* → SKILLS_LLM_* rompería el code assistant en prod (el litellmCompletionAdapter lee esas env de process.env por dentro). Fix en plan: PARAMETRIZAR el adapter (cada asistente pasa su config), no renombrar in-place.
- Plan actualizado: banner RESUELTO, §3.2 sin chatbotId, §5 GetItem id=agentId + accountId del row, §8 nota de coupling LLM, §13 #0 tachado.
- Estado: plan verificado contra código, SIN claims erróneos, SIN bloqueante. Abiertos solo 2 menores (locator edit_instructions con FE 7.1; validar flowId sí/no). Listo para CONTRACT.md o implementación.