---
tags: [claude-session, silia, billing, tax]
---

# Session: 2026-05-25 — Billing Audit Logs + Tax + RTA Sync

## Context
- **Branch:** feat/SL-682-audit-logs
- **Working directory:** /Users/sulli/Projects/silia/06-11-25/silia
- **Tickets:** SL-682 (Audit Logs), SL-1294 (US-TAX-01), SL-679 (Reactivate after payment)
- **Goal:** Finish audit log PR, implement automatic_tax, replace RTA HTTP sync with DynamoDB

## Work Log

### 1. Audit Log PR Review — accountId fixes
- Fixed `accountId: ''` in reactivateAgent → fetch chatbot by chatbotId, use chatbot.accountId
- Fixed `accountId: ''` in retryPayment → invoice.customerId → AccountModel.findByStripeCustomerId()
- Fixed `accountId: ''` in setDefaultPaymentMethod → body.customerId → AccountModel.findByStripeCustomerId()
- Fixed updateSubscription using Stripe `cus_xxx` customerId instead of internal accountId → same findByStripeCustomerId pattern
- Added GSI index ARNs (`/index/*`) to BillingGeneralRole IAM for BillingAuditLog (proactive for future reads)
- Added PutItem on BillingAuditLog to BillingUploadProofRole (was missing)
- User reverted audit log handler changes (linter removed imports) — handlers shipped without audit logging in some cases

### 2. US-TAX-01 — Stripe automatic_tax
- **Decision from Finance (May 2026):** Silia charges net USD for all 15 LATAM countries. WHT is client's contractual obligation. US clients → Stripe Tax auto-calculates Sales Tax per state.
- First implementation: conditional `automatic_tax: { enabled: country === 'US' }` in 5 places
- User corrected: should always be `{ enabled: true }` — Stripe returns /bin/zsh for non-US (no tax registrations configured) and calculates real amount for US
- Final: `automatic_tax: { enabled: true }` in all 5 places
- Removed unused variables: `scheduleCountry`, `subCountry`, `phaseCountry`
- Caught mistake: I unnecessarily moved `currentSubscription` retrieval outside conditional blocks for updateSubscription — reverted to keep it inside (avoids extra Stripe API call)
- Removed hardcoded MX=16% IVA: `taxRate = 0` everywhere (createSubscription.ts, Assistant/Put/index.ts x2)

### Files changed (StripeGateway.ts):
- Line 313: createSubscription → `automatic_tax: { enabled: true }`
- Line 389: createScheduledSubscription → `automatic_tax: { enabled: true }`
- Line 475: updateSubscription schedule path → `automatic_tax: { enabled: true }`
- Line 499: updateSubscription regular path → `automatic_tax: { enabled: true }`

### Files changed (StripeWebhookHandler.ts):
- Line 705: setup_intent.succeeded → `automatic_tax: { enabled: true }`

### 3. RTA Sync — Replace HTTP with DynamoDB
- **Problem:** `syncRtaStatus()` in AccountStatusService and AgentSuspensionService used HTTP calls to RTA internal API. Team said to modify DB directly.
- **Scope:** Only AccountStatusService.ts (user specified only touching AccountStatusService, StripeWebhookHandler, BillingService)
- Created lightweight `Accounts/domain/models/RtaConfig.model.ts` — extends DB base class, only has `id`, `agentId`, `status` + `findByAgentId()` + `setStatus()`
- Why not import from RTA submodule? RTA is a git submodule with its own webpack build — cross-submodule imports don't resolve
- Why not use AWSDynamoService directly? Nobody in the codebase uses it outside models — pattern is always model extends DB
- Updated AccountStatusService.syncRtaStatus() to use new model
- Added `RTA_CONFIGS_TABLE` env var to Accounts template Globals
- Added `dynamodb:Query,UpdateItem` on `-RTA-RtaConfigs` + indexes to RoleAccountsStatusUpdate

### 4. Checkov Baseline Updates
- Added 4 new entries for resources not in baseline:
  - `AgentConfigAuditTable` (CKV_AWS_119)
  - `IntentTable` (CKV_AWS_119)
  - `GetWhatsAppTemplates` (CKV_AWS_115/116/173)
  - `PatchChatbotConfig` (CKV_AWS_115/116/173)
- Renamed `BillingRateAuditTable` → `BillingAuditLogTable` (table was replaced)

## Files Modified This Session
| File | What changed | Why |
|------|-------------|-----|
| Billing/infrastructure/gateways/StripeGateway.ts | automatic_tax: true (4 places), removed country conditionals | US-TAX-01 |
| Billing/application/handlers/StripeWebhookHandler.ts | automatic_tax: true, removed phaseCountry | US-TAX-01 |
| Billing/infrastructure/aws/handlers/createSubscription/createSubscription.ts | taxRate=0 | US-TAX-01 |
| Assistant/application/Put/index.ts | taxRate=0 (2 places) | US-TAX-01 |
| Accounts/domain/models/RtaConfig.model.ts | New file — lightweight model for cross-module RTA config access | RTA sync |
| Accounts/domain/services/AccountStatusService.ts | syncRtaStatus: HTTP → DynamoDB via RtaConfigModel | RTA sync |
| Accounts/infrastructure/aws/aws.template.yml | RTA_CONFIGS_TABLE env + IAM permissions | RTA sync |
| .checkov.baseline | 4 new entries + 1 rename | CI fixes |

## Decisions Made
- automatic_tax always enabled — Stripe handles /bin/zsh for non-US via tax registration config
- taxRate initial = 0 for all countries (was MX=16%) — Stripe updates real value after invoice finalization
- Lightweight RtaConfig model in Accounts module vs importing from RTA submodule (webpack can't resolve cross-submodule)
- Use DB base class pattern (not raw AWSDynamoService) to match codebase conventions
- AgentSuspensionService NOT touched (user scoped changes to AccountStatusService only)

## Current State
- **Branch:** 8 commits ahead of develop, 4 uncommitted minor files (settings, utils)
- **Commits:** audit logs, PR fixes, tax, checkov baseline
- **PRs:** Tax PR created, audit log PR in review
- **CI:** Checkov baseline updated for new resources

## How to Pick Up From Here
1. Suspension FE task — analysis saved in memory (project_suspension_fe_analysis.md)
2. AgentSuspensionService still uses HTTP for syncRtaStatus — separate task to migrate
3. FE tasks: US-TAX-02 (show tax_amount in Billing > Agents), US-TAX-03 (invoice detail)
4. Audit log read endpoint not yet built — IAM GSI permissions already added proactively