# Feature 10 — CRUD de roles custom (SL-1289)

Rama: fix/SL-1289-default-roles-migration. Commits: 30afb6c96 (alta), 8f4fa47b2 (media), 6a6ae4818 (baja). Plan en docs/feature-10-roles-plan.md.

## Decisión de modelo (del PRD)
Catálogo de permisos atómicos = GLOBAL (definido por Silia, enum+tabla resource). Roles = híbrido: 5 default globales (accountId=null, isDefault=true), custom POR CUENTA (accountId set, isDefault=false). Aditivo, sin romper el sistema global actual. El provider (defineAbilitiesForRole) YA leía de DB → no había refactor código→DB pendiente. Feature flag de coexistencia = N/A.

## Subtareas (todas hechas)
- S1 RoleModel: accountId nullable, isDefault, description, createdBy, soft delete (deletedAt+ttl 90d), GSI accountId-name-index, finders findByAccountId/findByAccountAndName/countByAccountId.
- S2 seed-default-roles.ts idempotente (UPSERT por name, isDefault=true). NO inventa la matriz PRD.
- S3 create: unicidad/cuenta 409, límite 100 (422 role_limit_reached), permission_ids validados (422), crea role_permissions.
- S4 delete: soft delete, immutable por isDefault, bloqueo 409 con {users,teams} (grant read fail-open).
- S5 update: immutable por flag, diff de permisos, audit.
- S6 clone: 'Copia de X' (+sufijo), copia permisos, respeta límite. Función+ruta+build script (cloneMin.handler).
- S7 category en catálogo (ResourceModel + getCatalog). Valor vacío hasta sembrar de matriz validada.
- S8 límite 100 (en create/clone).
- S9 audit dedicado: RoleAuditLog model+service (fail-open), tabla+GSI accountId-timestamp+env, wired en create/update/delete/clone.
- S10 invalidación efectivos: PermissionInvalidationService.onRolePermissionsChanged (sync <=100, SQS >100), UserModel.listUserIdsByRole, envs REDIS+queue URL. Fail-open.
- S11 alerta interna >20 (log estructurado ALERT, sin webhook inventado).
- S12 resolución account-aware: defineAbilitiesForRole/resolveRoleId con accountId (custom por cuenta, fallback default global), cache key (role,account). DESBLOQUEA Feature 11.

Verificación: ~27 tests, tsc limpio en tocados, cfn-lint OK, security lint OK. Rutas mantenidas (/role, /permissions/catalog).

## Pendientes de producto (bloquean datos, no código)
1. Matriz ~102 permisos validada celda por celda → seed del mapeo rol→permiso (S2) y category (S7).
2. Discrepancia 'Implementador' (PRD) vs enum código (superuser/superadmin) — seed usa DEFAULT_ROLES del código, no inventé.
3. KMS: delete lee AccessGrant (CMK) para contar grants — necesita Opción A; fail-open si falla.
4. Canal Slack para alerta >20 (hoy log).